# Weekly Notes — 2026-07-13 to 2026-07-19

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md) · prev: [week-2026-07-06](week-2026-07-06.md)

> **Note:** took 07-13/07-14 off (end of the 4-day break) — week starts 2026-07-15.

---

## Week Summary

**Short week (07-15–07-17) that moved the IL plan from design to a working, validated
collection pipeline — then found and reported the two bugs that block using it.**

**Shipped & robot-validated:**
- **Task reset poses (plan 1.a)**: `setpose`/`usepose`/`home`, staged arms→fingers reset
  with a 2 s finger ramp, deploy-consistent by construction. Designed 07-15, built+sim
  07-16, robot-validated 07-17.
- **Single-side teleop** (`pico_world_wuji_right`, profile `sides:` key): right arm+glove
  teleoped, left welded to the task pose, left hand re-commands the captured grasp every
  tick. **Parquet-proven IL-clean**: left-hand action = bit-identical non-zero grasp
  constants; left-arm action deviation 0.0. (Caught + fixed a would-be train/deploy
  landmine: a `None` idle hand records ZEROS → deploy would open the hand.)
- **Camera 60 fps**: two root causes — missing `--camera-serial` grabbed the robot's
  built-in cam; the hub's A→C converter passes USB3 in ONE plug orientation (marked).
- Rebased everything onto teammates' new 50 Hz control stack same-day (`wip/collection-
  integration-20260717`), suite green.

**Found, root-caused, reported (collection currently blocked):**
1. Glove retargeting saturates on finger movement → correlated dual-side stalls → strict
   recorder fails episodes ~every 15–20 s. Quantified with `--deep` captures.
2. Driver playout faults + unintended robot motion (⚠️); likely = upstream's SCHED_FIFO
   stutter fix silently denied on our Orin. rtprio experiment pending.
   → [issues/2026-07-17-shakedown-issues.md](../issues/2026-07-17-shakedown-issues.md)

**Working-style lessons banked:** test on integration branches, merge after green;
collect under the team's experimented settings — failures under those settings are the
report, don't tune around them; error messages can blame the wrong component
("missing left hand data" ≠ left hand's fault).

**Next week:** 🎯 07-25/26 pipeline-final deadline + on-site demo sessions. Waiting on
labmates' fixes for the two blockers; rtprio experiment; **HR operator scheduling
(overdue, first thing)**; then curriculum data collection (plan 1.b) for real.

---

## Plan (set 2026-07-15) — next big goal: IL / autonomous execution of the cube-twist task

> The teleop side of the two-step Rubik's-cube twist task works (vertical two-finger twist →
> horizontal twist). Next big goal: **imitation learning → autonomous execution** of it.

**Substeps:**

1. **1.a — Reset-logic change — ✅ BUILT + SIM-VERIFIED 07-16** (`feat/task-reset-pose`;
   capture via `setpose`, reuse via `usepose`, staged arms→fingers reset; details in HANDOFF).
   Remaining: glove/robot validation (07-17) + team discussion before upstreaming.
   *07-15: current mechanism mapped + design options written up →
   [design/recording-reset-pose.md](../design/recording-reset-pose.md).*
   Today pressing `r` always ramps to the default init pose (arms at side of body, pointing
   forward). The task is hard enough that episodes should instead START from a pose where the
   **left hand is already grasping the cube**. Non-trivial change touching teleop, data
   collection, AND deployment (the deploy-time reset must reproduce the same start pose, or
   we create a train/deploy mismatch). Think it through carefully and discuss before building.
2. **1.b — Collect data + train, curriculum-style.** Start with the easiest substep (left hand
   pre-grasped, right hand single-finger twist); gradually increase task complexity and see
   how far IL gets.
3. **1.c — Risk hedge: RL reading.** The task may be too challenging for pure IL — read recent
   RL-for-dexterous-manipulation works in parallel (on the other laptop → notes repo is now
   committed + pushed each session so it's readable there).
4. **2 — Data-collection bug.** Teammates report **a few seconds of data lost here and there
   during recording**. If they aren't prioritizing a fix, investigate it ourselves (it blocks
   trusting collected data for 1.b).

**Decide focus AFTER the team meeting** (see meeting prep below).

---

## Meeting prep (upcoming team meeting)

Questions to ask, so we can pick our first focus:

1. **Current status:**
   - Is Quest integration done / in progress? (They suggested switching PICO → Meta Quest for
     teleop; requires large code changes.)
   - Is data replay really fixed on the newer branch? Which branch?
2. **Their current plan:**
   - Which bug(s) are they focused on fixing right now?
   - Is someone already working on the data-recording bug (seconds lost during collection)?
   - Will the main architecture be largely refactored soon? (Affects whether we build the
     reset-logic change now or wait.)

---

## Open TODO

> Single source of truth — updated each session. Check off items when done, add new ones as they come up.

**Session-start (07-15, first session after the break):**
- [x] **`git pull` in we-teleop** — done 07-15: switched to `develop` (teammates' instruction); full snapshot on `wip/pre-develop-switch-20260715`; restored exposure fix (develop still had the bug), torso lock, full previous `wuji_right.yaml`, 4 policy configs
- [ ] **⚠️ NEXT ROBOT SESSION: Orin NOT synced yet** — run `scripts/setup/sync_luna.sh nvidia@6.6.7.100` before starting the driver (workstation is on develop, Orin still on old master tree)
- [ ] **Labmate's verdict on the jitter parquets** (`rubric-cube-0710__*`, sent 07-10) — follow up; confirm they know state is 60 Hz sampled (>30 Hz aliases).

**New (2026-07-17, post-meeting):**
- [ ] **🎯 DEADLINE 2026-07-25/26 (next weekend): teleop pipeline FINAL** — then start on-site
  sessions at the designated location doing the required demo tasks (recurring afterwards).
- [ ] **1a. Pipeline shakedown before 07-25** — exercise the full teleop+recording pipeline
  hard; keep a running issue list; route each issue to the responsible teammate EARLY (they
  need lead time to fix before the deadline). **Running list + evidence:
  [issues/2026-07-17-shakedown-issues.md](../issues/2026-07-17-shakedown-issues.md)** —
  already holds 2 blockers (unintended movement w/ driver playout faults; episodes failing
  on hand-data gaps) + upstream hygiene items.
- [ ] **1b. HR coordination** — contact HR colleagues to assign + schedule the operator(s)
  for the 07-25/26 on-site sessions and the recurring demo-task slots after. Don't sit on
  this — people need to block their weekend.
- [x] **2. Merge teammates' new rewrite-0711 work BEFORE collecting data** — DONE 07-17 pm (`wip/collection-integration-20260717`, suite green) — big low-level
  changes upstream: control 60→50 Hz / driver 100 Hz (1:1 with Gento's internal servo loop;
  their comment: 120 Hz commands caused a 10–35 Hz tracking-error beat — plausibly OUR
  一卡一卡!), arm accel 30→60, PICO resample/smoothing rework, hand-EE smoothing, Orin
  desched stutter fix, wuji-sdk==2026.6.18 pinned upstream (labmate branch merged),
  `driver_address` now in profiles. Collecting at 60 Hz then deploying at 50 Hz = exactly
  the train/deploy gap we avoid. Rebase our 6 commits (3 task-reset-pose + single-side +
  camera-check exposure + finger ramp) onto the new tip on a fresh integration branch.

**New (2026-07-16):**
- [ ] **⚠️ Every operator shell: `unset __NV_PRIME_RENDER_OFFLOAD` before teleop** (or patch `robo.nix` locally — needs full shell exit/re-enter) — upstream 07-15 pull sets it + `hostGraphics: nixgl-nvidia` for a hybrid laptop; on our desktop (RTX 4090 = primary display GPU) it makes the MuJoCo window render all black. Verified: unset → renders fine.
- [ ] **Upstream-report list grew**: robo.nix graphics not host-portable (suggest host-configurable `hostGraphics` instead of hardcoded nixgl-nvidia + PRIME offload) — 3rd "works-on-their-machine" instance after D405 exposure units + D405-only camera profile

**New (2026-07-16 pinch/tactile session):**
- [x] **Pinch strategy vs finger-TWISTING interference** — CONFIRMED 07-16: right hand `d1=d2=0` (snap off) made twisting much better, another task succeeded. **TAKEAWAY: pinch threshold strategy is TASK-DEPENDENT** — twist-heavy tasks want the snap OFF (d1=d2=0), pinch/grasp tasks want normal (~1.5/2.0 cm). We may need per-task retarget configs or a runtime toggle — raise with labmate (fits his calibration/finetune-store work: maybe per-task overlays)
- [ ] **Tactile gates disabled 07-16 in BOTH hand yamls** (`pinch_pressure` + `press_grip`, `p_on: 1.0 / p_full: 2.0`) — new glove pair reads ~0.28 phantom pressure at rest (labmate agreed to turn gate down). Re-enabling blocked on trusting glove tactile: capture per-finger rest noise (`we hands check --deep`), report to labmate/vendor. ⚠️ if re-tuning, keep `p_full > p_on` (inverted ramp locks gate ON)

**New (from 2026-07-15 planning):**
- [x] **Design the recording reset-to-grasping-pose change (plan 1.a)** — ~~sketch options~~ BUILT + sim-verified 07-16 (`feat/task-reset-pose`): pose captured live (`setpose`), operator-owned, deploy reproduces it by construction (same RESET path). Robot validation 07-17; present to team before upstream
- [ ] **PICO controller reset before every teleop session** — teammates' interim suggestion: reset controllers via the ring(?) button pre-session — verify which button/procedure with them
- [ ] **Ask teammate: Quest switch progress** (→ meeting) — until then PICO stays; third-person-view tracking is shaky, hanging over the operator's neck works better
- [ ] **Data replay: pull the newer branch and retest** — teammates say the replay breakage is fixed there (supersedes our `--max-frames`/`.rrd` workaround ideas — verify first)
- [ ] **Data-recording data-loss bug (a few seconds lost intermittently)** — ask at meeting who owns it; if unprioritized, investigate ourselves
- [ ] **Commit + push this notes repo at end of every session** (readable from the other laptop for the RL reading thread)

**Carried from last week (see [week-2026-07-06](week-2026-07-06.md) for full context):**
- [ ] **PICO jitter stationarity A/B** — *deprioritized 07-15:* new observations (third-view shaky, neck-worn better) + possible Quest switch; revisit only if we stay on PICO and the controller-reset tip doesn't help
- [ ] **Test B of finetune store** — `mv ~/.we-teleop/wuji_finetune.json.off` → on, rerun sim; watch the right-pinky `d1 7.95` suspect
- [x] **Exposure fix follow-ups** — ~~upstream a `--camera-exposure` flag~~ DONE 07-16 (commit `16cd156`, ready to push): labmates' 10 ms D405 default kept, our D455 passes `we driver --camera-exposure 100`; camera.py local patch RETIRED. Still open: pin `rs.option.gain` (optional)
- [ ] **Left glove tactile sensor FAULTY (hardware)** — vendor contacted 07-08, still silent; follow up on replacement
- [ ] **Adjust default torso pose LOWER** — operator wants a higher relative view; no higher chair, so lower the robot's default torso pose *(may fold into the 1.a reset-pose redesign)*
- [ ] **PICO dims/sleeps every 5–10 min on rack** — interim = nudge every few minutes *(moot if Quest switch happens)*
- [ ] **Bimanual ergonomics for cube twisting** — seeing the right hand's working fingers forces a tiring left-hand pose; rethink view/grip arrangement
- [ ] **Glove cable extension proper fix** (when reach needed) — active repeater / powered hub / ethernet-side extension; passive USB3 confirmed broken
- [~] **Eden's teleop tasks / objects** — picking up items since 07-09; fold into this week's task list
- [ ] **Right pinky pinch threshold anomaly** — loose gate (`d1 7.95 / d2 9.95`); if it mis-triggers, re-run `calibrate_wuji_hand.py --hand right --pinch-fingers pinky`

**Software / infra (carried over):**
- [ ] Lock `enp13s0` static to `192.168.31.48` + clear stale `192.168.123.123`
- [ ] Background board — ask W or S
- [ ] Verify hand opens on `Tab` reset in new deploy (hand-reset fix not re-applied upstream; *directly relevant to 1.a — reset-pose design must decide hand state too*)

**Hardware (carried over):**
- [ ] Arm droop under hand weight (no brake on end motors) — reported to TIANJI, awaiting response
- [ ] Camera mount proper fix — D455 still taped
- [ ] Quest headset setup *(now the teammates' suggested direction — see meeting prep)*
- [ ] 3D scan robot head structure — scan with iPhone, send to 3D-modeling owner

**Teleop quality (carried over):**
- [ ] PICO wrist calibration — redo once mounts are stable

---

## Daily Log

### 2026-07-17

**Evening — collection BLOCKED by two root-caused pipeline bugs; both reported to
labmates with full evidence. Week closed here.**

- Attempted real collection on the new 50 Hz tree → episodes kept failing within
  seconds. Systematic isolation (operator experiments + instrumented captures) found
  TWO separate defects:
  1. **Retarget stall on finger movement** (workstation): the per-frame optimizer hits
     its 50-eval cap on EVERY moving frame (9→50 evals, 3.6→24 ms median, tails >150 ms);
     both gloves stall in lockstep (shared supervisor-process GIL — identical gap
     timestamps both sides); publish gaps >100 ms occur 3–4/min → strict recorder kills
     episodes every ~15–20 s. Trigger isolated BY EXPERIMENT: hands moving w/ fingers
     still = fine; any finger motion = instant fail. + sub-bug: error always blames
     "left hand" regardless of stale side.
  2. **Driver playout faults + unintended robot movement** (robot host, ⚠️ safety):
     `interpolation lookahead rejected` every 100 Hz tick while the arm moved with the
     controller still; feedback also going stale (forced pauses). Root-cause candidate:
     upstream's desched fix (`78248c1`) requests `SCHED_FIFO/20` — our Orin DENIES it
     (startup warning, runs SCHED_OTHER) → the stutter it fixes persists here.
     **rtprio-grant experiment pending (tomorrow):** `echo 'nvidia - rtprio 20' | sudo
     tee /etc/security/limits.d/99-we-teleop-rtprio.conf`, re-login, driver restart,
     expect `policy=SCHED_FIFO` in startup line.
- Full evidence + analysis in [issues/2026-07-17-shakedown-issues.md](../issues/2026-07-17-shakedown-issues.md)
  (verbatim logs, quantified `--deep` capture `hand_check_0717.jsonl`, suggested fixes).
  Both bugs sent to labmates ✓ — waiting on their fixes; collection on hold.
- Also fixed en route: left-glove cabling (real but separate issue — new cables, was
  the pre-session `carrier=0`), `input_timeout_ms` briefly raised then REVERTED
  (collect under the team's experimented settings; failures under those settings ARE
  the report — rule saved).
- ⚠️ NOT done this week: **HR coordination for the 07-25/26 on-site operators** —
  deliberately deferred, but it has a human-latency clock; FIRST THING next session.

**Midday — ROBOT-VALIDATED ✓: task reset pose + staged hand reset + single-side teleop +
recording loop, all working on hardware.** The full plan-1.a + collection stack is live:

- **`setpose rubik_test` captured on-robot** (bimanual grasp of the cube); staged reset
  verified with real hands: body ramps → converge → fingers close → settle. `usepose
  rubik_test` re-activates in any new session (pose persists at
  `artifacts/task_poses/rubik_test.yaml`; ⚠️ activate BEFORE the first `Tab` in a session,
  else reset ramps to default init while the left hand keeps gripping).
- **Finger snap fixed mid-session** (user feedback: close too fast) → fingers now RAMP from
  measured position onto the captured targets over 2 s (`_RESET_HAND_CLOSE_NS`) + 1 s
  settle — commit `a53ee67`. Body ramp feels 一卡一卡 (slow reset envelope; watch, not
  blocking).
- **Single-side teleop validated on robot** (`pico_world_wuji_right` + `usepose`): right
  arm+glove teleoped, left arm welded to the pose, left hand keeps the grasp, left
  controller asleep on the rack — no effect on teleop. Full `R`/`S` recording loop ran.
- **Parquet PROOF (dataset `rubik_test_v0__20260717T105348`)**: `action.hand.left` =
  701/701 rows bit-identical non-zero grasp constants ✓ · left-arm action max deviation
  0.0 ✓ · right side varies normally ✓. The zeros-at-deploy hazard is closed.
- **Camera mystery solved**: driver `--camera-name tianji_head_d455 --camera-exposure 100`
  streamed the ROBOT'S BUILT-IN head cam — profile sets resolution/fps only; without
  `--camera-serial` the pipeline grabs the first enumerated RealSense. Fix: always pass
  `--camera-serial 419222302053` (our D455). Also explains the `we camera check` timeout
  (built-in cam + D405-unit exposure default); check tool got an `--exposure` flag
  (`86780d9`, needs Orin re-sync to use there).
- ⚠️ Today's `rubik_test_v0` episodes recorded the WRONG camera view — throwaway only.

**Afternoon — camera 60 fps UNBLOCKED: the USB2 bottleneck was the A→C converter's
plug ORIENTATION.** Saga: driver with the right serial failed `Couldn't resolve requests`
→ D455 enumerated at 480 Mbps (USB2) behind the hub → hub itself stuck as "USB2.1 Hub"
(SuperSpeed bus empty) → suspected converter/hub → tech staff tip: cheap A→C converters
wire only ONE orientation's SuperSpeed pairs → **flipped the hub-uplink plug 180° in the
converter → hub now enumerates as USB3.2 @ 5000M, D455 @ 5000M ✓**. Notes:
  - **Converter orientation MARKED** (do not replug blind — wrong way = silent USB2,
    driver fails at 60 fps).
  - Camera-side flip does nothing — the cap was on the hub UPLINK, not camera→hub.
  - Topology check (from workstation): `ssh nvidia@6.6.7.100 'for d in /sys/bus/usb/devices/*; do [ -f "$d/product" ] && echo "$(cat $d/product) | $(cat $d/speed) Mbps"; done'`
    — pass = hub AND D455 at 5000.
  - ⚠️ **Powered USB3 hub purchase still recommended**: whole chain is bus-powered off one
    ~900 mA port (camera ~700 mA peak + 2 glove endpoints) — same power class as the 07-15
    glove blackout/xHCI wedge. Shopping notes in the 07-17 chat: 独立供电, 全端口 5Gbps
    数据, A口上行 preferred, per-port switches nice for glove power-cycles.
  - Orin has TWO physical port groups in play (buses 3/4 + 5/6), not one as assumed.

**Morning — single-side teleop BUILT (runbook step 0.5) → `ac81a41` on the integration
branch.** No `git fetch` today by choice (frozen tree for the collection session).

- New profile key `sides` (default `[left, right]`) + profile **`pico_world_wuji_right`**:
  right arm+glove teleoped; left arm frozen at the task pose (only selected wrists are IK
  targets/active joints — solver provably keeps inactive joints at seed, regression test
  added); left glove not connected/required (`we hands check --side right` already existed).
  Resets stay whole-body. Teleop: `scripts/runtime/pico.sh pico_world_wuji_right --driver-address 6.6.7.100`.
- **⚠️ SPEC CORRECTION (important for IL data):** idle hand action must NOT stay `None` —
  `None` is recorded as ZEROS and fails the required-modality check; a policy trained on
  zeros would OPEN the left hand at deploy. Fix: control loop fills the idle side each tick
  from the active task pose's hand targets → recorded action = applied command = grasp
  constants. Details in [design/single-side-teleop.md](../design/single-side-teleop.md).
- PICO mapper previously required BOTH controllers tracked or it emitted NO action — now
  side-aware, so the left controller can really stay on the rack.
- Suite green in operator profile except the 3 known failures (upstream `pico_world head:
  pico` bug + our 2 loosened-init-gate locals); ruff clean. Headless single-side run OK.
  Also fixed a stale test from yesterday's `37f7706` (record_task message wording).
- Untracked `config/teleop/keyboard_right_tmp.yaml` left in tree for an interactive sim
  check (drive right arm, verify left frozen at `setpose` pose) — **delete after checking**
  (its presence fails the pinned profile-list contract test).

### 2026-07-15

**Planning session (back from the break).** Reorganized notes + set the next big goal.

**Progress correction for last week (notes lagged behind the actual sessions):**
- ✅ **Two-step Rubik's-cube twist task teleoperated successfully** — step 1: two-finger
  vertical twist; step 2: horizontal twist. (Notes had it at "a lot better"; it got to
  working end-to-end.)
- ⚠️ **PICO tracking in third-person view is still a bit shaky.** Hanging the headset over
  the operator's neck improves it. Teammates suggest **switching to Meta Quest** for teleop
  (large code change on their side — ask progress at the meeting). Interim tip from
  teammates: **reset the PICO controllers (ring button?) before every teleop session.**
- ⚠️ **Data replay still not working on our side** — teammates say it's fixed on a newer
  branch; pull + verify.
- ⚠️ **Data-collection bug (from teammates):** a few seconds of data intermittently lost
  during recording.

**Afternoon — URGENT: reproduce teleop tasks + videos for supervisor. Codebase moved to `rewrite-0711`:**
- Meeting outcome: latest code is `rewrite-0711` (NOT develop) — big rewrite: schema v10, four-process operator, new `we driver run --robot-address <gento-ip>` CLI, strict episode rules (**pause/reset mid-episode now FAILS the episode**), new preflights (`we camera check`, `we hands check`), driver can be remote-started/synced from the operator (`--driver-setup sync --start-driver`).
- Switched workstation to `rewrite-0711` (b8d0d7f "Release we-teleop 0.2.0"). Snapshot of patched develop: `wip/develop-patched-20260715`.
- **Patches re-applied (3):** middle `d2: 3.0` (rewrite already ships colleague's tuned config — our delta was ONE line); `enable_torso: false` in profile (torso lock is a first-class key now); **hand serials in `config/actuators/wuji.yaml`** — ⚠️ rewrite shipped the LABMATES' serials (387E.../3482...), replaced with ours (L=366239543134, R=306735523434). Old env-var exports are gone.
- **Exposure patch NOT needed** — exposure-lock code removed entirely in the rewrite (auto-exposure back); verify camera view on robot. Policy configs NOT restored (new strict policy families; not needed today; on snapshot branches).
- Network check after weekend router change: PICO LAN fine (`.48` alive, router pings, `TELEOP_XROBOT_HOST_IP=192.168.31.48` unchanged). ⚠️ robot ethernet `enp14s0` NO-CARRIER (robot was off — recheck at power-on).
- Old Gento controller address from old driver code: `6.6.7.190` (x86 box) → `--robot-address 6.6.7.190`.

**Planning output:** next big goal = IL / autonomous execution of the cube-twist task —
see the Plan section above (reset-to-grasping-pose change → curriculum data collection +
training → RL reading as hedge; recording data-loss bug on the radar). Focus to be finalized
after the team meeting (questions in Meeting prep).

**Housekeeping:** new week file created; README milestone log updated (dual-arm/dual-hand
setup + cube-twist teleop); HANDOFF refreshed; notes repo committed + pushed (previously the
weekly-notes dir + CHEATSHEET were untracked — other laptop couldn't see them).

**Late afternoon/evening — the urgent-video gauntlet (2 task videos captured ✓):**

*Result: task 1 (from the original set) + a multi-object pick-and-place filmed and ready for
supervisor. Harder tasks continue tomorrow morning. Right-arm-only from mid-afternoon (see
hardware below).*

Everything hit on the way, in order (full patch set: [rewrite-0711-local-patches-20260715.diff](../implementation/rewrite-0711-local-patches-20260715.diff)):
1. **Orin env for rewrite-0711**: no internet (weekend router change) → NoMachine + new WiFi;
   nix fetch OK. Then `ImportError: typer_main` → `sync_luna.sh` does NOT refresh the venv —
   needed `robo run -p tianji-driver -- uv sync --locked --extra camera --extra wuji-hand`
   (from `bootstrap_luna_orin.sh:15`). SSH keys set up (`ssh-copy-id nvidia@6.6.7.100`) — remote
   driver lifecycle + passwordless sync now work.
2. **Camera "Couldn't resolve requests"**: rewrite's `daa65c0` re-introduced the exposure lock
   (`exposure_us=10_000` = 1 s on D455 100µs-units — units bug round 2!) AND the default profile
   is D405-only 640x480@90 (D455 max = 60). Patched both (camera.py exposure 100 + new
   `tianji_head_d455` profile) — but **Orin driver mysteriously didn't see the new cameras.yaml
   after sync** (unresolved — investigate before data collection). Ran `--no-camera` all day.
3. **Init-pose gate too strict for our robot**: rewrite fails startup unless max error ≤0.5° AND
   vel ≤1°/s; our wrists hold ~0.7° steady-state under hand weight → can never pass. Patched
   `interfaces.py` to 2.0°/5°/s. (Our actual startup error: ~1.0°.)
4. **Glove USB blackout mid-session**: hub disconnect wedged the xHCI controller
   (`couldn't allocate usb_device`) → fix = PCI unbind/rebind of `0000:12:00.4` (no reboot needed).
5. **⚠️ LEFT HAND MOUNT MELTED** — left hand nearly fell off; dismounted. Dismount also knocked
   out shared hand power/USB (right hand vanished from bus → loose connector, re-plugged).
   → Right-only driver patch (`driver_supervisor.py` `sides=("right",)`).
6. **Left arm PD timeout after dismount**: the old ARM_TOOL bug is BACK on this path — non-zero
   tool dynamics on a tool-less arm times out mode switching (TIANJI "fix" evidently didn't
   cover it). Patched `gento.py`: zero tool dynamics for left arm only, right keeps hand config.
7. **Robot ignored all teleop (silent!)**: debug capture (`artifacts/logs/driver-*/`) showed every
   command `rejected=left hand command has no actuator` — operator still streams left-glove
   targets; the rejection holds the WHOLE robot. Patched `hands.py` to ignore targets for
   unmounted sides. ← *upstream-worthy: rejection-vs-ignore + a driver `--hand-side` flag.*
8. **Right glove signal degraded** (reconstructed hand looked wrong) → swapped to another pair
   end-of-day. ⚠️ **Today's pinch d2 re-tuning (index 2.5 / middle 2.2) was done against the BAD
   glove — re-test with stock values (4.0/4.0 + our 3.0 middle) on the new pair tomorrow.**

Also learned/confirmed: rewrite = PD mode automatic (arms/torso, evolved 07-03 gains in
`model.py`), head position mode; torso lock works (`enable_torso: false`, verified in logs);
head tracks the neck-worn headset (looks like "torso moving" — head-lock patch drafted then
reverted, revisit); driver debug goes to files not terminal; recording REQUIRES image modality
(no episodes without camera); teleop keys now UI reset/resume; `we driver reset-errors`
replaces the 上位机 for simple clears.

### 2026-07-16

**Morning — left hand back, upstream pull, black-window fix:**
- **Left hand REMOUNTED** (mount reprinted). Reverted the 3 right-only patches
  (`driver_supervisor.py` sides, `hands.py` ignore-unmounted, `gento.py` zero-tool-dynamics)
  via `git checkout --` — each file held only its WARN-marked hunk. Keeps: serials, cameras.yaml
  d455 profile, pinch yaml, interfaces.py init-pose gate, camera.py exposure.
- **Pulled upstream `rewrite-0711`** `f1ea07e → 7983e6e` (8 commits, clean fast-forward, no
  rebase). Route: drop doomed profile edit → stash → `merge --ff-only` → pop (zero conflicts).
  Notable incoming: **1-euro filter + smoothing for PICO jitter** (labmate working our jitter
  report!), extrapolation toggle in `runtime.yaml` (marked untested), impedance mode back
  (same kp/kd as pd), wandb eval QoL, big `we hands` CLI expansion, `CLAUDE.md` agent map.
- **Teleop profiles renamed/simplified**: `pico_ee_absolute_wuji_hands` DELETED → now
  `pico_world_wuji_hands`; new schema keys `arms/head/torso/hands`. Torso lock re-expressed as
  `torso: hold` (was `enable_torso: false`; loader accepts only `hold`/`ik`). New command:
  `scripts/runtime/pico.sh pico_world_wuji_hands --driver-address 6.6.7.100`.
- **MuJoCo window all black after pull — ROOT CAUSE: robo.nix graphics change.** Upstream set
  `hostGraphics: nixgl-nvidia` + `__NV_PRIME_RENDER_OFFLOAD=1` (+ dropped egl-wayland) — PRIME
  offload is for hybrid laptops (teammate's machine); our workstation is a desktop with the
  RTX 4090 as PRIMARY display GPU (X11 already renders on NVIDIA, glxinfo verified) → offload
  context = black window. **Fix verified: `unset __NV_PRIME_RENDER_OFFLOAD` → renders fine.**
  Needed in every new operator shell until robo.nix is patched locally or fixed upstream.
- deps unchanged in pull (`pyproject.toml`/`uv.lock` untouched) — no uv sync needed; but
  `robo.nix`/`flake.lock` changed → full shell exit/re-enter on both machines; Orin re-synced?
  (do `sync_luna.sh nvidia@6.6.7.100` before next driver start if not yet done).

**Late afternoon — SDK ROOT CAUSE CONFIRMED + task-reset-pose feature built:**
- **Retargeting fine on wuji-sdk 2026.6.18** (labmate's branch pin; rewrite-0711 locks 2026.7.2
  which "altered glove calibration handling" per his pyproject note). Strongly suggests the
  07-15 "bad right glove" + today's phantom pressure were **SDK artifacts, not hardware** —
  revisit: gloves may be fine, tactile gates may be re-enableable on 2026.6.18.
- **Full test suite run: 3 failures, all explained:** (1) `test_launch_profiles` = GENUINE
  upstream bug (test expects `pico_world` `head: hold`, config ships `head: pico` — fails on
  clean rewrite-0711 too; → labmate report list; note our local head/torso hold edits will trip
  its `pico_world_wuji_hands` assert as well once fixed); (2)+(3) `test_driver_startup` +
  `test_reset_lifecycle` = expected local noise from our loosened init gate in interfaces.py
  (tests pin upstream 0.5°/1°/s) — permanent fix = upstream a configurable gate.
- **Task-specific reset pose DONE** (plan 1.a): branch `feat/task-reset-pose` (`5e59053` +
  `37f7706`). `setpose <pose>` capture · `usepose <pose>` activate (decoupled from `rc`
  dataset name) · `home` default reset. Details in HANDOFF.
- ⚠️ **Tomorrow's tree must combine**: rewrite-0711 + labmate's branch (SDK pin!) + feature
  branch — merging labmate's or at least its `pyproject`/`uv.lock` pin is REQUIRED or uv sync
  re-upgrades to broken 2026.7.2.

**Evening — feature SIM-VERIFIED ✓ + workflow lesson:**
- **Branch restructure after a fair callout**: I (Claude) had merged the untested feature
  straight into local rewrite-0711 — wrong order. Fixed: rewrite-0711 reset to origin (clean);
  all testing on **`wip/collection-integration-20260716`** (= tip + labmate branch + feature +
  local patches). Rule saved to memory: test on an integration branch, merge only after green.
- **Sim tests PASSED** (keyboard profile, no gloves): unit suite for the feature + live flow —
  `setpose test1` → move → `Tab` returns to captured pose → `home` returns to default →
  `usepose` reactivation → full recording loop (`rc` → `usepose` → `R` auto-record at pose →
  `S` saves; episodes land in `artifacts/data/robot/`). Exactly the intended UX.
- **`usepose` added** (user feedback): pose name decoupled from `rc` dataset name — capture a
  task pose once, collect any number of datasets against it.
- **Staged reset added** (user request): body ramps first, fingers close last (+1 s settle)
  when the pose carries hand targets — UNTESTED (needs gloves), first thing to check tomorrow.
- Full tomorrow runbook in HANDOFF. Notes repo: commit + push at session end!
