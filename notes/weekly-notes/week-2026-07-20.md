# Weekly Notes — 2026-07-20 to 2026-07-26

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md) · prev: [week-2026-07-13](week-2026-07-13.md)

---

## Week Summary

**🏁 Milestone week: FIRST autonomous IL success on the cube-twist.** After the 07-22
strategy pivot (more + cleaner data, no return-to-initial, temporal aggregation), the v1
policies trained (07-23 relaunch after a `num_workers:32` DataLoader segfault) and on
**07-24 the live closed-loop eval hit 16/16 successes** — v0's OOD/compounding failure is
gone. Full pipeline (teleop → collect → repair → train → deploy) works end to end. Known
limit, as expected: with no corrective behavior in the demos, an off-pose cube makes the
policy retry without adjusting → next data batch needs recovery / cube-position variation.

**🗓️ Timeline moved: demo shoot POSTPONED to Thu 2026-07-30** (setup 07-29). So **07-27 +
07-28 are two working days to finalize the pipeline and cut the stable demo version** — the
07-25/26 crunch is relieved. Finalize items: stable branch, reset-grip softening, camera
frame-drop fix. (07-24 also lost ~half a day to a hardware network outage — a physically
loose NIC card, [[workstation-nic-card]].)

---

## Week Focus

Carried in from last week's close ([week-2026-07-13](week-2026-07-13.md) · [HANDOFF](../../HANDOFF.md)):

1. ~~**HR coordination (OVERDUE)**~~ **DONE 07-21** — operators locked; on-site sessions
   start **Mon 07-27** (recurring after). Overdue blocker cleared.
2. **rtprio experiment on the Orin** (Issue 2 candidate fix, ~5 min, no operator needed) —
   grant `rtprio 20`, verify `policy=SCHED_FIFO` in driver startup, re-test still + moving
   episodes, report to driver owner.
3. **Track labmates' fixes for the two collection blockers**
   ([issues/2026-07-17-shakedown-issues.md](../issues/2026-07-17-shakedown-issues.md)):
   (1) retarget stall on finger movement (collection blocker);
   (2) driver playout faults + unintended movement (⚠️ safety).
   When fixes land: re-run the finger-movement repro + 5-episode collection smoke before trusting.
4. **Then real cube-twist collection** (plan 1.b curriculum) — pipeline otherwise READY
   (runbook in HANDOFF §PICK UP NEXT / CHEATSHEET §4).

---

## Open TODO

> Single source of truth — updated each session. Check off items when done, add new ones as they come up.
> Carried over from [week-2026-07-13](week-2026-07-13.md); done items pruned.

**Deadline-critical:**
- [ ] **🔴 07-28 (Tue) PREP DAY** — (1) 🔴 rehearse the offline nix→SSD migration on the spare
  workstation, network unplugged ([runbook](../checklists/2026-07-29-onsite-provisioning-runbook.md));
  (2) operator batch: torso teleop re-check (sim→robot) + hand A/B with TWO stable hand yamls
  (pinch-enhanced vs plain; manual swap = `cp` preset + restart, no switch mechanism);
  (3) hardware transport inventory (adapters gated on TIANJI's port answer — pack 2× USB-eth +
  hub + spares). Detail in the 07-27 daily log. Shoot 07-30, setup 07-29.
- [ ] **On-site workstation provisioning (milestone)** — TELEOP-ONLY offline SSD seed (~13 GB);
  千兆网 but NO VPN → foreign caches (cache.nixos.org/GitHub/HF) unreliable. Runbook + the 3
  must-rehearse unknowns: [checklists/2026-07-29-onsite-provisioning-runbook.md](../checklists/2026-07-29-onsite-provisioning-runbook.md).
- [ ] **Await replies:** TIANJI workstation specs (esp. **NVIDIA?** — gates the MuJoCo viewer) +
  wifi/firewall + Wed access; labmate on torso/head teleop enable + safety.
- [x] **🎯 PIPELINE VALIDATED (07-24): 16/16 live closed-loop eval** — autonomous cube-twist
  works end to end (v0's OOD failure gone). Deadline crunch relieved: **demo shoot postponed to
  Thu 07-30** (setup 07-29) → **07-27/28 = finalize + cut the stable version.**
- [x] **HR coordination DONE (07-21)** — operators locked; **on-site sessions start Mon 07-27**
  (recurring after). Advisor gave the TIANJI-side demo POC (for the shoot coordination).
- [ ] **🛒 ASSET PURCHASE for the 07-30 demo shoot (LEAD-TIME ITEM — Yihe owns comms)** —
  shoot postponed to Thu 07-30 (setup 07-29) → a little more runway, but STILL coordinate
  buy-vs-already-have and get orders moving (shipping clock). ⚠️ STANDING REMINDER until closed.
- [ ] **Pipeline shakedown before 07-25** — keep exercising the pipeline; running issue list
  w/ evidence: [issues/2026-07-17-shakedown-issues.md](../issues/2026-07-17-shakedown-issues.md)
  (2 blockers reported 07-17, both fixed upstream 07-20); route new issues to owners EARLY.
- [ ] **⚠️ NEW (07-20): PICO right-controller tracking snap → arm lurch (safety)** — fully
  root-caused from a recorded episode, evidence package ready to send:
  [issues/2026-07-20-pico-tracking-snap.md](../issues/2026-07-20-pico-tracking-snap.md).
  Report to teleop owner: suggest task-space step clamp; feeds the Quest decision.
  Interim: controller reset pre-session, watch occlusion, hands OUT of workspace.
- [ ] ~~rtprio experiment~~ **DEPRIORITIZED 07-20**: Issue 2's real cause was the retarget
  boundary margin (fixed upstream) — SCHED_FIFO denial on the Orin still worth reporting
  to the driver owner as a separate observation, not as the Issue-2 fix.
- [ ] **Robot re-validation on the merged tree (07-20)**: finger-movement repro (Issue 1)
  + 5-episode collection smoke + staged reset + single-side + `usepose` before trusting.
- [ ] **⚠️ Thumb finetune overlay is the labmates' calibration** (`wuji_finetune.json` now
  in-repo, always loads, `thumb_abd_track w: 30`) — recalibrate ours
  (`scripts/calibration/calibrate_thumb_spread.py`) or zero `w` before teleop; raise with
  retargeting owner.
- [ ] **Tactile gates: recalibrate with the new baseline flow** (`calibrate_wuji_hand.py`
  now subtracts per-finger rest baseline) then restore real `p_on`/`p_full` — our
  gates-OFF workaround kept in the merge until then.

**Session-start / standing:**
- [ ] **`git pull` in we-teleop at session start** (tree currently frozen on
  `wip/collection-integration-20260717`; check labmates' pushes for the two bug fixes first)
- [ ] **⚠️ Orin + workstation must stay in lockstep** — after any workstation tree change:
  `scripts/setup/sync_luna.sh nvidia@6.6.7.100` (+ venv refresh if deps changed; exit/re-enter
  robo shells if flake.lock changed)
- [ ] **⚠️ Every operator shell: `unset __NV_PRIME_RENDER_OFFLOAD` before teleop** (until
  robo.nix patched — black MuJoCo window otherwise)
- [ ] **Commit + push this notes repo at end of every session**

**Carried over (active):**
- [ ] **Labmate's verdict on the jitter parquets** (`rubric-cube-0710__*`, sent 07-10) — follow
  up; confirm they know state is 60 Hz sampled (>30 Hz aliases). Also owe them the 50 Hz
  一卡一卡 feel verdict once teleop is usable again.
- [ ] **Tactile gates disabled in both hand yamls** — re-enabling blocked on trusting glove
  tactile on SDK 2026.6.18; capture per-finger rest noise (`we hands check --deep`), report.
  ⚠️ if re-tuning, keep `p_full > p_on`.
- [ ] **Per-task pinch-threshold configs** — snap OFF (d1=d2=0) for twist-heavy tasks, normal
  (~1.5/2.0) for pinch/grasp tasks — raise with retargeting labmate (fits his finetune-store
  work: per-task overlays?).
- [ ] **PICO controller reset before every teleop session** (ring button? — verify procedure
  with teammates)
- [ ] **Data replay: pull the newer branch and retest** — teammates say replay breakage fixed
  there; verify before our `--max-frames`/`.rrd` workarounds.
- [ ] **Test B of finetune store** — `mv ~/.we-teleop/wuji_finetune.json.off` → on, rerun sim;
  watch the right-pinky `d1 7.95` suspect.
- [ ] **Buy a powered USB3 hub** (bus-power budget = 07-15 blackout-class risk; shopping
  checklist in [week-2026-07-13](week-2026-07-13.md) 07-17 afternoon log). ⚠️ A→C converter
  orientation MARKED — do not replug blind.
- [ ] **⚠️ Init-gate error TREND — right wrist steady-state error GROWING, watch it:**
  | date | measured | joint | gate set to |
  |---|---|---|---|
  | 07-15 | ~0.7° steady / ~1.0° startup (wrists, under hand weight) | wrists | 0.5°→2.0° |
  | 07-20 | **2.01°**, vel 3.26°/s | **Arm_R6_Joint (right wrist)** | 2.0°→2.5° |
  ~3× growth on the same joint class in 5 days. Each new occurrence: record joint + error
  + velocity from the driver line before bumping anything. If it hits ~2.5° again or keeps
  growing → stop bumping, investigate instead: right-hand load/mount changed? PD gains /
  dynamics compensation actually active for the hand? arm droop (no brake, TIANJI ticket
  open) getting worse? Related upstream ask: configurable init gate.
- [ ] **Upstream-report list** — robo.nix graphics not host-portable; init gate not
  configurable (now w/ 07-20 evidence: Arm_R6 2.01° under hand load); `head: pico` profile
  contract test; JPEG-quality test stale; merge-backup files committed on rewrite-0711;
  error blames "left hand" regardless of stale side.
- [ ] **Left glove tactile sensor FAULTY (hardware)** — vendor silent since 07-08; follow up.
- [ ] **Adjust default torso pose LOWER** (operator wants higher relative view) — may fold
  into task-pose captures now that `setpose` exists.
- [ ] **Verify hand opens on `Tab` reset in new deploy** (upstream hand-reset regression;
  reset-pose feature decides hand state at reset — check the default-`home` path too).
- [ ] **Ask teammate: Quest switch progress** — until then PICO stays (neck-worn beats
  third-person view).

**Deprioritized / waiting:**
- [ ] PICO jitter stationarity A/B — only if we stay on PICO and controller-reset tip fails
- [ ] PICO dims/sleeps on rack — interim: nudge every few minutes (moot if Quest)
- [ ] Bimanual ergonomics for cube twisting (view/grip arrangement)
- [ ] Glove cable extension proper fix (active repeater / powered hub / ethernet-side)
- [~] Eden's teleop tasks / objects — fold into demo-task list
- [ ] Right pinky pinch threshold anomaly (`d1 7.95 / d2 9.95`) — recalibrate if it mis-triggers

**Software / infra (carried over):**
- [ ] Lock `enp13s0` static to `192.168.31.48` + clear stale `192.168.123.123`
- [ ] Background board — ask W or S

**Hardware (carried over):**
- [ ] Arm droop under hand weight (no brake) — awaiting TIANJI response
- [ ] Camera mount proper fix — D455 still taped
- [ ] Quest headset setup (teammates' suggested direction)
- [ ] 3D scan robot head structure — iPhone scan → 3D-modeling owner

**Teleop quality (carried over):**
- [ ] PICO wrist calibration — redo once mounts are stable

---

## Daily Log

### 2026-07-20

**Morning — labmates' fixes merged: `wip/collection-integration-20260720` built + suite
green. Both 07-17 blockers addressed upstream.**

- **Upstream dropped 16 commits on rewrite-0711** (`dfddafb → 09b7273`), including fixes
  for BOTH our reported blockers — and their root-causing matched/corrected ours:
  1. **Retarget stall (Issue 1)**: they confirmed our GIL diagnosis (two per-side SLSQP
     solves serializing in the supervisor process, ~25 Hz both-sides) → **one child process
     per glove side** (`we-hand-left`/`we-hand-right`) + retarget iteration changes + thumb
     retargeting rework.
  2. **Playout rejection storm (Issue 2)**: NOT SCHED_FIFO — the retarget boundary margin
     (1e-4 rad) emitted targets a few mrad above the connected hand's actual joint limit →
     100 Hz rejection storm. Fixed: margin 5e-3, rejection reason now propagated+latched.
     **rtprio experiment likely moot for this issue** (desched stutter may still be real,
     but don't run it as an Issue-2 fix).
  - Bonus fixes: strict recorder no longer fails episodes on hand dropouts (rows kept with
    `left/right_hand_available` markers; LeRobot conversion REJECTS unavailable rows — no
    zeros training hazard); phantom tactile pressure root-caused (glove-wear pressure at
    finger base) → **calibrated baseline subtraction** in `calibrate_wuji_hand.py`; wuji
    serial auto-discovery (`left_serial: auto`); pusht/eval/wandb QoL.
- **⚠️ Upstream built PARALLEL versions of our features** (semantic overlap, resolved):
  their "single-hand teleop" = network preflight allows one CONNECTED glove (hardware
  availability); ours = profile `sides:` key welding an arm to the task pose (task
  design) — merged so profile sides restrict + their reachability discovery filters.
  Their "reset pose" = one global `wuji_reset_pose.yaml` folded into `start_action`;
  ours = per-task `setpose`/`usepose` with staged body→fingers reset — both kept, ours
  takes over when a task pose is active.
- **Merge route: fresh `wip/collection-integration-20260720`** = origin tip + our 9
  commits cherry-picked + 1 restore commit. ~19 conflict hunks resolved (worst:
  control_process.py). **One real regression caught by our own test**: upstream's reset
  drives hands from the command but still ramped the BODY to the driver's hardcoded start
  pose — task poses need the commanded target; restored (`dcab849`) — without it `usepose`
  would silently reset to default init. **Suite: 536 passed / 8 failed — all 8 accounted**
  (6 pre-existing on clean upstream tip, verified by baseline worktree run; 2 = our known
  init-gate locals). Ruff clean, supervisor smoke green, headless local sim clean.
- **⚠️ NEW works-on-their-machine hazard: `config/inputs/retargeting/wuji_finetune.json`**
  — the finetune store moved INTO the repo and always loads; the committed file carries
  the LABMATES' thumb-spread calibration (`thumb_abd_track w: 30.0`, their gains) → will
  actively drive OUR thumbs with their per-glove calibration. Before next robot session:
  run `scripts/calibration/calibrate_thumb_spread.py` on our gloves, or zero `w` locally.
  Raise with retargeting owner (per-machine data in a shared config file).
- **Retargeting yamls**: kept our tactile-gates-OFF + pinch 1.5/2.0 for now — upstream's
  baseline fix only works after RECALIBRATION (default baseline = zeros = old broken
  behavior). Proper path: recalibrate with new baseline flow, restore real thresholds.
- Serials: ours re-applied (left explicit, not `auto` — their checked-in right anchor is
  their hardware; ambiguous discovery fails closed with both sides foreign).
- **Before next robot session**: `pyproject.toml`/`uv.lock` changed → Orin
  `sync_luna.sh nvidia@6.6.7.100` + venv refresh (no flake changes, shells fine).
  Old `wip/collection-integration-20260717` untouched = restore point. NOT pushed.

**Midday — robot session: init-gate trip (fixed) → ⚠️ NEW unintended-movement issue,
fully root-caused: PICO right-controller tracking snap.**

- Driver init gate tripped at startup: `Arm_R6=2.01°` vs our 2.0° gate → bumped to 2.5°
  (trend table in TODO — 3rd bump, watch it). Teleop then hit **two arm lurches with the
  controller still**; recorded the episode and root-caused from the parquet: right-wrist
  target teleports 7–10 cm with **0° rotation** while left controller unaffected = VIO
  tracking loss + position reacquisition snap on the right controller. Pipeline/driver
  CLEAN (0 faults). Labmates confirmed: same view/occlusion issue as before — left hand
  lurked pre-180°-view-flip, right hand after. Evidence package:
  [issues/2026-07-20-pico-tracking-snap.md](../issues/2026-07-20-pico-tracking-snap.md).
- wuji-sdk 2026.6.18→**2026.7.14** (firmware-matched — glove/hand firmware updated to
  labmates' version). Left glove calibrated with new baseline flow (rest pressure
  0.31–0.44 confirmed real; gate re-enabled in baseline-subtracted units). ⚠️ right-hand
  calibration DIDN'T SAVE — re-run `calibrate_wuji_hand.py --hand right --tactile-check`;
  right still carries labmates' thumb `w:30` — zero/delete before robot use. Thumb spread
  calibration deliberately deferred (yaml default w=0 on left).
- `sim_glove_to_hand.py` ported to new retarget API (`received_monotonic_ns` passthrough);
  left-hand sim check: **~121 Hz steady retarget** (Issue-1 fix visible), press_gate ~0 at
  rest ✓. Watch: index pinch_alpha 0.70 at rest — fitted `d1 3.49` may be loose.
- Native rerun viewer WORKS on this tree (replay breakage fixed upstream ✓).

**Afternoon — QUEST TELEOP WORKS IN SIM, zero code changes 🎉**

- Path: Quest device from support (dev mode already ON — no account/org needed) →
  `adb install XRoboToolkit-Quest-1.0.1.apk` (prebuilt from XR-Robotics
  `XRoboToolkit-Unity-Client-Quest`, v1.0.1) → Quest onto teleop Wi-Fi (was on SZX-office;
  teleop LAN = 192.168.31.x) → app → `192.168.31.48` → sim teleop (`pico_world` + local)
  tracks correctly. **Our "PICO" receiver is headset-agnostic** (talks xrobotoolkit_sdk);
  PC-service pin v1.0.0-2 speaks to the Quest client fine.
- ⚠️ Quest tracking has strict controller-angle/visibility requirements too — sometimes
  controllers not visible. Evaluate placement/rig before declaring the snap problem
  solved; the task-space step clamp suggestion stands regardless of headset.
- **NEXT: 3D-print the Quest mount** (blocks the rig), then robot-validate Quest teleop.
- Picker gotcha for sim: the `driver address:` prompt is PRE-FILLED with `6.6.7.100` —
  clear it fully before typing `local` (or pipe: `printf '3\nlocal\n' | pico.sh -- uv run
  we teleop --select-profile`).

**Late afternoon — session cut short (unwell), quick status + next-session plan:**

- **PICO snap: NOT battery** — teammates checked, ruled out; tracking-loss theory from
  the 07-20 evidence package stands. They suggest **trying a different PICO headset
  angle/placement tomorrow** — pending, not yet tested.
- **Quest mount**: 3D printing in progress (to fix Quest to the glove/wrist rig) — no
  session time needed while it prints; check next session.
- **New: 07-27 (Mon) demo shoot needs prep** — multiple shots planned. Missing info:
  what props/equipment the team already has vs. what needs buying. **Action for next
  session: ask teammates for an inventory before buying anything** — don't duplicate
  purchases. Nothing else scoped yet (shot list, task selection, run-through) — do this
  early in the week, not the night before.
- Session ended early (07-20) — not feeling well. No robot/glove state left mid-change;
  safe to resume from this file next time.

### 2026-07-21

**Morning — coordination wins + Quest sim check; real-robot path deferred to afternoon.**

- ✅ **HR/operators LOCKED** — on-site sessions start **Mon 07-27** (overdue blocker cleared).
  Advisor supplied the TIANJI-side demo POC.
- ✅ **Quest mount printed + installed** on the operator rig — Quest robot teleop unblocked.
- ✅ **Quest teleop re-checked in SIM** (operator wearing Quest + gloves) — looked fine on a
  brief look. ⚠️ **Gotcha:** `--driver-address local` FLAG does NOT normalize to sim — only
  the interactive `--select-profile` prompt does (cli.py:411 vs the flag path at cli.py:209).
  Symptom: control comes up, then `RuntimeError: paced command publisher failed` ~3 s in
  (took the remote-ZMQ branch, control_process.py:455, to a peerless "local" endpoint).
  **Sim launch = `pico.sh <profile> --select-profile` then Ctrl-U + type `local`.** Decided
  NOT to patch the flag (minor; keep code churn down). Candidate one-line fix if it keeps
  biting: normalize `resolved_driver` "local"/"none"→None in the non-selector path too.
- **⚠️ Upstream dropped ANOTHER large refactor on rewrite-0711** (`09b7273 → f27ea68`, 17
  commits, **239 files, +23.5k/−10.2k**) — CLI split (`we/cli.py`→driver/eval/policy/train
  modules), `control_process.py` rewrite, `embodiments.yaml` strict schema-v2 (no v1 compat),
  reset unification, configurable Tianji IK. **DECISION: do NOT pull this week** — it's a
  refactor not a hotfix, would eat the collection window, and re-collides with our task-reset
  + init-gate work (3rd time upstream built parallel versions). Our 07-20 tree is validated +
  unblocked. **Two fixes worth a surgical cherry-pick IF their symptom appears during
  collection:** (1) *IK joint-boundary-normalization* — IK returning a joint one float-ULP
  past its limit → `rejected unsafe human action` storm mid-recording (touches only
  `solver.py`); (2) *wuji-runtime-reconnect* — glove-dropout watchdog + auto-reconnect. Also
  *configurable-IK* eventually replaces our local init-gate patch. Post-demo: coordinate with
  the owner to converge our reset/init-gate work with theirs rather than re-merging each time.
- **Robot NOT powered this AM** (was in sim) → Orin `6.6.7.100` unreachable (no ping/SSH) →
  **sync + driver + real teleop are an afternoon job.** Nothing left mid-change.
- Operator left for lunch; real teleop resumes PM once robot is up.

**Lunch-window tasks (no Orin/robot needed — glove connects to the workstation):**
- Right-hand recal (interactive; relax→press each fingertip) + zero the right
  `thumb_abd_track w:30` in `wuji_finetune.json`.
- Quest-rig fitting (proper headset placement for the operator — camera visibility, not just
  ergonomics; Quest has controller-visibility limits too).
- 🛒 Asset-purchase comms (Yihe) — get orders moving (lead time).

**Afternoon — TIANJI comms sent; starting real-robot Quest teleop.**

- 🛒 **Messaged the TIANJI demo POC (07-21 PM)** — asked: (1) hardware/teleop-stuff
  availability, (2) cable-management requirements, (3) assets-they-have vs. need-to-buy
  (pushing asap so operators can practice), (4) WiFi/router, (5) **robot setup — is the
  on-site unit configured like our rig** (Wuji hands + mounts + firmware). Detailed infra
  questions deferred to the follow-up. ⏳ awaiting reply. STILL open: concrete buy-list with
  arrival date + who-purchases.
- Quest pipeline re-verified in sim (again) — good. Proceeding to real-robot Quest teleop.

**Afternoon — Quest teleop on the REAL robot ✅ + first single-side collection (50 eps) +
calibration fixes. Pipeline correct; ONE blocker = camera frame-drops.**

- ✅ **Quest teleop on the real robot** — no-hands tracking check first (`pico_world`, head+torso
  locked), then hands+camera (`pico_world_wuji_hands` / `pico_world_wuji_right`). Zero code
  changes; our receiver is headset-agnostic. New mount+angle: tracking held, **snap not
  reproduced this session** (keep watching before declaring it solved).
  - Local edit: `config/teleop/pico_world.yaml` head/torso → `hold` (wuji profiles already had it).
- **Thumb over-sensitivity ROOT-CAUSED + FIXED live:** it was the labmates' `thumb_abd_track`
  on the RIGHT (`w:30`, `j2` gain −2.7) driving the thumb ~2.7×. Removed → right thumb on
  default, matches left (both `w=0`). **A/B tested** (temporarily restored w:30): confirmed
  it gives more thumb flexibility = the right DIRECTION, but overshoots on our glove (their raw
  fit). **Proper fix = `calibrate_thumb_spread.py` on OUR right glove** (fits a correct overlay).
- **Right-hand recal DONE + SAVED** (`calibrate_wuji_hand.py --hand right --tactile-check`) —
  07-20 save failure resolved. **Both hands now freshly calibrated per-glove** (pinch thresholds,
  segment scaling, pressure baseline; tactile gates re-enabled). Numbers differ L vs R = per-glove
  fit, expected. ⚠️ right `d2` came out large (index 5.56 / middle 6.51) — watch for over-sticky
  fingers; lower that finger's `d2` in `wuji_finetune.json` if so.
- **First single-side cube-twist collection: 50 episodes** →
  `artifacts/data/robot/2026-07-21-15-54-54__rubik_twist_v0` (pose `rubik_twist_test`).

**⚠️ DATASET CHECK — pipeline CORRECT, but camera frame-drops block training:**
- ✅ 50 Hz solid (dt 20.00 ms); `command_mode=teleop` 100%; both hands available 100%.
- ✅ **single-side invariant PERFECT**: `action.hand.left` = constant non-zero grasp (per-dim
  std 0.0, NOT zeros); `action.hand.right` = live varying twist. Action/state data is clean.
- ⚠️ **D455 dropped 24 frames MID-episode** (median 68% in, none at boundaries) → **20/50 eps
  flagged "faulty"** (`missing_required_image`), 30 clean.
- ⚠️ **Training impact:** LeRobot conversion REQUIRES all-clean (`src/teleop/data/lerobot.py:141`).
  `we data curate` repeats missing **actions/hands** across short gaps but **NOT images**
  (`REPETITIVE_FAULT_FLAGS`, `schema.py:72` — images excluded on purpose; won't fabricate a
  frame). So the 20 faulty eps CAN'T be auto-repaired → **trainable NOW = 30 clean eps only.**
- **Root cause / fix (TOP NEXT):** mid-episode drops at 640×480@60 = USB3 bandwidth/power (A→C
  converter orientation + still-unbought powered hub). (1) verify D455 on USB3 5000M (CHEATSHEET
  §1 sysfs check); (2) if bandwidth-bound, drop camera to **30 fps** (control is 50 Hz, recorder
  stores latest frame → fine). Fix → clean-rate ~60%→~100%. **Don't scale collection until fixed.**
- Upstream-report adds: camera-frame-drop; curate can't repair `missing_required_image` (by
  design — surfaces as a hard "faulty" wall); `--driver-address local` flag not normalized to sim.

**Evening — #2 DONE: missing-frame repair → all 50 eps trainable. #1 (big merge) = tomorrow.**

- **New feature (branch `feat/repair-missing-frames`):** `scripts/data/repair_missing_images.py`
  — forward-fills `missing_required_image` rows with the previous valid frame (backfills a
  leading gap), sets `image_available`, clears the fault, adds a `forward_filled_image` marker,
  re-marks episodes clean. Standalone script (does NOT touch shared curation/recorder — which
  the big merge rewrites tomorrow). Rationale: a dropped frame shouldn't lose a whole episode.
- **Ran + verified on the 50-ep set** → `…__rubik_twist_v0_repaired`: all 50 clean, 32,761 rows,
  **0 missing images**, fill confirmed (repaired frame == previous), single-side invariant intact.
  Ready for LeRobot conversion / training (needs a policy config pointing at it — next step).
- **#1 UPSTREAM MERGE = planned milestone for TOMORROW** (07-22): user wants it done (fast-iterating
  project, can't wait a week), but treated as a large milestone — Claude examines the 239-file
  refactor carefully and proposes a STAGED plan for review before executing. ⚠️ Conflict scope
  mapped today: ~14 source files incl. `control_process.py` (rewritten upstream → re-implement),
  `src/we/cli.py` (split into modules → re-port our CLI changes), `driver.py` (collides w/ their
  reset-unification + configurable-IK). ⚠️ Can't fetch/push from Claude's env (HTTPS creds) →
  Yihe fetches/pushes; ⚠️ upstream built PARALLEL reset/single-hand versions → coordinate with
  owner on whose wins before pushing our feature to the shared branch.
- **Training TEED UP + dry-run VERIFIED:** created `config/policy/act_rubik_twist.yaml` (mirrors
  the baseball hands ACT config, points at the repaired dataset, `include_hands: true`). ⚠️
  **Training venv was STALE** — its `we` launcher imported the old `we.cli:typer_main`; current
  entry is `we.cli:main` (operator venv already refreshed, training wasn't). Fixed with
  `robo run -p training -- uv pip install -e . --no-deps` (regenerates the launcher without
  pruning torch/lerobot). `we train act_rubik_twist --dry-run` then resolved cleanly
  (lerobot-train, cuda, 50k steps, batch 32). **Ready: `robo run -p training -- we train
  act_rubik_twist`.** If the launcher goes stale again → same fix, or a full training re-sync.
- ⚠️ **2nd training blocker (single-side gotcha):** LeRobot hand conversion (`lerobot.py:168`)
  requires BOTH `left_hand` + `right_hand` in the recording's `required_modalities`, but
  single-side recordings only declare `right_hand` (the live glove) — the idle LEFT hand is
  filled-from-pose so the recorder never lists it, even though its data is complete + 100%
  available. Fix: declare fully-available hand sides in `required_modalities`. Folded into
  `repair_missing_images.py` (declares any hand side whose `*_hand_available` is all-True) +
  applied in-place to the current `_repaired` set → `we train` conversion now passes.
  ⚠️ dry-run does NOT exercise conversion, so it passed while the real run failed here.
  Upstream-report: single-side recorder should declare the filled idle hand as a modality.
- ⚠️ **3rd blocker (repair-script bug, fixed):** rewriting parquets via `set_column(name,…)`
  reset the touched fields to `nullable=True` (originals are `nullable=False`) → LeRobot
  `episode 0 Arrow schema differs from metadata` during conversion. Fixed: `_repair_table`
  captures the original schema and `table.cast(original_schema)` before write. Regenerated
  `_repaired`; verified rewritten-episode schema == untouched-episode schema. **Three fixes
  total to get training past conversion: stale venv launcher, left_hand modality, schema
  nullability.** Re-run training after clearing the stale LeRobot cache from the failed run.
- **NEW feature `feat/replay-initial-pose` (built 07-21 eve, sim-verified, HARDWARE-UNTESTED):**
  `we data replay ... --pose <name>` starts PAUSED with the task pose pre-activated → `Tab`
  ramps to it (staged, place cube) → `Enter` plays the recording. Reuses the existing RESUME
  path (which already does replay begin_approach+play) + staged-reset. 3 files: `control_process.py`
  (config field + conditional auto-play + paused-start + pose activation), `supervisor.py`
  (OperatorConfig field + wiring), `runtime/cli/data.py` (`--pose` option). Verified: ruff clean,
  task-pose/single-side tests pass, headless smoke shows `mode=pause` + pose activated. ⚠️ Also
  fixed a PRE-EXISTING replay crash (`replay.task`→`replay.dataset_label` — `we data replay` was
  fully broken on this tree). ⚠️ 2 supervisor_smoke fails are pre-existing/env (my edits guarded
  by replay_pose). ⚠️ OPEN: hands profile for replay w/o gloves. ⚠️ re-port after the big merge.

### 2026-07-22

**Deploy debugging → two new features committed → policy goes OOD → strategy pivot to more +
cleaner data.**

- **Replay validated on the real robot** (`we data replay --pose`): ran clean; effect imperfect —
  the twist is precision-critical, so a small reset offset compounds through the rollout.
- **⚠️ Deploy env gap (root-caused, recipe in memory):** hardware policy deploy needs **operator +
  train-lerobot + wuji-glove in ONE venv**; NO robo profile has all three (operator lacks
  train-lerobot, training lacks wuji-glove). Unblock = superset `uv sync` into the training venv:
  `robo run -p training -- uv sync --extra operator --extra train-lerobot --extra pusht --extra
  huggingface --extra wuji-glove`, PLUS the `xrobotoolkit` `PYTHONPATH`/`LD_LIBRARY_PATH` exports
  (pico.sh normally sets these; `we deploy` doesn't). Run from the SAME shell — a fresh
  `robo shell -p training` re-syncs and strips wuji-sdk. ⚠️ the deploy guard mislabels ANY
  ImportError as "requires the train-lerobot extra" — read the per-process errors instead.
- **⚠️ `we_policy.json` gotcha:** `observation_features` hides the auto-injected egocentric camera
  (conversion always adds `observation.images.egocentric`). `act_rubik_twist` IS a vision+state ACT
  (checkpoint `pretrained_model/config.json` → `input_features`). Deploy NEEDS the camera; replay
  and `we eval`/`--gt-obs` do not. Ground truth = the checkpoint config, never `we_policy.json`.
- **NEW FEATURE — `we data replay --pose`** (`cbd02af`, local, NOT pushed; already logged 07-21).
- **NEW FEATURE — gt-obs HARDWARE deploy** (`4c851b5`, local, NOT pushed): `we deploy <ckpt>
  --gt-obs <ds> --driver-address <host>` feeds the policy RECORDED observations while publishing its
  actions to the robot (open-loop). Was offline-metrics-only on this tree; ported the idea from old
  master. Separates a visual/sim-to-real gap from closed-loop compounding. Seam = recorded state/
  image/seed injected into `PolicyActionProvider` only (publish path untouched); shared frame loader
  `load_recorded_policy_frames` mirrors `evaluate_open_loop`'s projection; `--pose` for the staged
  start. Verified: ruff, imports, loader (62-dim state), CLI dry-run, offline path intact;
  supervisor-smoke fails confirmed pre-existing via stash. Hardware path operator-run ✓.
  ⚠️ touches `control_process.py` → re-port after the big merge. ⚠️ CLAUDE.md owning-doc update owed.
- **Policies:** `act_rubik_twist` (v0 50-ep, 50k, chunk 100). Trained **`act_rubik_twist_chunk200`**
  (30k, chunk 200; config `config/policy/act_rubik_twist_chunk200.yaml`, NOT committed; final loss
  ~0.052). Deploy verdict: **live rollout goes OOD into unseen states → weird**; chunk200 only
  slightly better. **gt-obs (recorded-obs) hardware deploy ~75% success** — policy ACTIONS from clean
  obs are mostly right, misses are the precision ceiling. Open-loop eval MSE ~5e-5 (near-perfect on a
  training episode).
- **🎯 STRATEGY PIVOT (agreed):** (1) collect **MORE data — target 200 eps**; (2) **NO
  return-to-initial** in demos (kills the reach-vs-retract state ambiguity + shortens episodes);
  (3) enable **temporal aggregation at inference** (`we deploy --temporal-aggregation`, existing flag).
- **Initial pose re-captured BIMANUALLY** (`pico_world_wuji_hands` → `/ setpose`): operator rejected
  the single-side grasp. New initial pose = **`rubik_twist_horizontal_v1`**
  (`artifacts/task_poses/rubik_twist_horizontal_v1.yaml`, captured 15:03; supersedes a first
  `rubik_twist_horizontal` at 14:58). **USE THIS pose for the v1 collection.**
- **BATCH 1 collected → `artifacts/data/robot/2026-07-22-15-10-03__rubik_twist_horizontal`: 97 eps,
  50 Hz, 89 clean / 8 faulty (~8%)** — far better than v0's 40% faulty; the no-return strategy →
  shorter episodes → much less mid-episode camera-drop exposure. Pose `rubik_twist_horizontal_v1`.
- **⚠️ HARDWARE: LEFT hand faulted mid-batch-1 → STOPPED at 97.** F1J2/F1J3 over-temperature then
  `F1J3 transmission slip detected` (critical, "possible hardware damage") from sustained hard
  cube-grip. Verbatim log + timeline: [issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md](../issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md).
  **HMI inspection later looked OK** (no obvious fault reported there) — **but still plan to contact
  Wuji for assistance** given the transmission-slip critical. Mitigation TODO: reduce commanded grip
  force in the `rubik_twist_horizontal_v1` pose / retargeting so it doesn't recur.
- **BATCH 2 collected → `artifacts/data/robot/2026-07-22-15-46-12__rubik_twist_horizontal`: 100 eps,
  50 Hz, 89 clean / 11 faulty.** Same pose `rubik_twist_horizontal_v1`, no-return strategy.
  **COMBINED batch 1 + 2 = 197 eps, 178 clean** (~6× v0's 30 clean).
- **NEXT (new conversation):** **merge the two horizontal batches for training** (both 50 Hz, same
  pose/strategy) — either a combined LeRobot conversion or a policy config that trains on both
  dataset roots. Then train a fresh config (`include_hands`), re-deploy WITH `--temporal-aggregation`,
  compare vs v0/chunk200. ⚠️ camera frame-drops still unfixed (batch 1 ~8%, batch 2 ~11% faulty);
  fix at source (USB3 5000M / 30 fps / powered hub) when there's a window.

### 2026-07-23

**Light day (unwell). Coordination + a failed eval attempt that uncovered a training bug → fixed
+ relaunched; replay validated on hardware. No successful eval yet (blocked on retraining).**

- **Coordination:** demo project says hardware goes **onsite tomorrow (07-24)** — discrepancy vs our
  "sessions start Mon 07-27"; messaged advisor to confirm (1) my scope, (2) on-site decision-maker,
  (3) hard deadline. 🛒 asset buy-list still open.
- **Built the 07-24 pre-demo teleop checklist**
  ([issues→](../checklists/2026-07-24-pre-demo-hardware-check.md)): two hand-retarget presets
  (enhanced-pinch vs pinch-off — ⚠️ NO switch mechanism exists, loader reads fixed `wuji_{side}.yaml`;
  needs building), thumb naturalness (coord w/ retargeting owner), body/torso lower pose (⚠️ torso is
  all-or-nothing `hold`/`ik` — 6 leg joints; no per-joint lock, no height param; static captured pose
  = safe fallback), smoothing (1-euro; **hand smoothing IS merged** `hands.py:119` + `lp_alpha`;
  values are hardcoded Python not yaml), and **⚠️ CHASSIS (底盘) not in the teleop pipeline** — CLAUDE.md
  says nonzero chassis commands fail-closed (no verified Luna base transport). Film plan has shots
  needing base + upper-body moving together → raise with owner + advisor EARLY.
- **Replay validated on real robot** (`we data replay … --pose rubik_twist_horizontal_v1`): reset
  looked fine. ⚠️ Glove connection had a **loose physical cable** (WujiException connection timeout at
  192.168.1.100/.101) — replugged, fine. ⚠️ Under `--profile keyboard` (hands `hold`) the hand doesn't
  reset on `home` (control process doesn't manage the hands → driver holds last grasp); use a wuji
  profile for in-session `home`, or bounce the driver (hands open on shutdown, `b81567c`).
- **⚠️ EVAL BLOCKED — the v1 policies never trained.** Tried `we deploy … --gt-obs` → `policy metadata
  not found: …/we_policy.json`. Investigated: BOTH horizontal runs **crashed overnight at 13k/20k of
  100k** with `DataLoader worker … killed by signal: Segmentation fault`. Root cause = **`num_workers:
  32`** in the policy configs — ~32 concurrent video decoders segfault (crash step varied run-to-run
  = concurrency, not a bad frame). `we_policy.json` is only written on completion, so partial runs
  aren't deployable. Saved as memory [[train-dataloader-segfault]].
- **Fix + relaunch:** `num_workers 32→4` + `steps 100k→50k` in both configs; moved crashed dirs to
  `*_crashed20k`/`*_crashed10k`; **fresh** runs (NOT `--resume` — resume restores the saved 32-worker
  config) launched sequentially in tmux `train` (chunk-100 `&&` chunk-200), started 18:43, running
  overnight. Confirmed alive (wandb `run-20260723_184310`, GPU 75%). Milestone to watch = clearing 20k.
- **Corrected stale deploy commands** (CHEATSHEET/HANDOFF still had June pick-toy syntax): `--live` and
  `--ckpt-name` are GONE on this tree — live = default when `--driver-address` set & no `--gt-obs`;
  checkpoint = `--checkpoint-name`/`--checkpoint-step`. Profile for the v1 eval = **`pico_world_wuji_right`**
  (dataset meta confirms single-side `sides:[right]`, NOT bimanual). ⚠️ **Deploy must run `we deploy`
  DIRECTLY, not `pico.sh -- we deploy`** — pico.sh re-runs the PC-service cmake build which picks the
  training venv's pip `cmake` and dies with `GLIBC_2.38 not found`; export the prebuilt xrobot paths
  instead (recipe in [[deploy-venv-gap]], memory updated with this symptom).
- **Uncommitted edits (for the stable branch):** 2 policy configs (num_workers/steps),
  `config/teleop/pico_world.yaml` (head/torso hold), `wuji_finetune.json` (recal), plus the earlier
  `repair_missing_images.py`. Stable-branch cut + CHEATSHEET fix still TODO (07-23 item #3, not done).

### 2026-07-24

**🎉 FIRST AUTONOMOUS IL SUCCESS: 16/16 live closed-loop cube-twist — full pipeline validated.
Morning eaten by a hardware network outage (loose NIC card); afternoon was the payoff.**

**Morning — network outage, root cause = a physically loose NIC card (~half a day lost).**
- Nothing reachable — robot (`6.6.7.100`) and teleop LAN both down after the workstation ethernet
  was unplugged/replugged during a wifi fix. Long misdiagnosis chasing cables, sockets, router,
  DHCP-vs-static, and NetworkManager config — all red herrings.
- ⚠️ **Symptom pattern to recognize next time:** one port (`enp13s0`) linked at gigabit with 0
  errors **but nothing on it ever answered** (DHCP timeout, ARP INCOMPLETE, no ping), while the
  other port (`enp14s0`) showed **carrier=0 / "cable unplugged" no matter what**. Both ports live
  on the **same add-in NIC card** → the card had worked loose. Wifi (`wlp5s0`, separate) staying up
  made it look like "just the wifi card." **Reseat the card FIRST.** Saved as memory
  [[workstation-nic-card]].
- Fixed by reseating (colleague): robot pings on `enp14s0` (`6.6.7.166`), `enp13s0` pulls
  `192.168.31.48` via DHCP, router (`192.168.31.1`) reachable. ⚠️ during debug I'd temporarily put
  the robot IP on enp13s0 → duplicate `6.6.7.166` on both ports; reverted enp13s0 to DHCP.

**Training — both v1 policies DONE (recovered from the overnight stall).**
- `act_rubik_twist_horizontal` (chunk-100): 50k ✓, `we_policy.json` written 20:06 (07-23).
- `act_rubik_twist_horizontal_chunk200`: 50k ✓. It had **stalled ~15 h overnight** when the network
  dropped (process alive, tqdm timer still counting, but not stepping); recovered ~11:16 when the
  DataLoader workers respawned, then finished clean. The `num_workers:32→4` fix held (both cleared
  20k, no segfault). Checkpoints are local-only, so the network outage never threatened them.

**Afternoon — 🎯 EVAL: 16/16 live closed-loop successes. v0's OOD failure is GONE.**
- Setup: driver on Orin w/ camera (`--camera-serial 419222302053`); deploy from the training
  **superset** shell ([[deploy-venv-gap]]), running `we deploy` DIRECTLY (not via pico.sh → GLIBC
  crash), `unset __NV_PRIME_RENDER_OFFLOAD` for the MuJoCo window. Profile `pico_world_wuji_right`,
  both gloves connected, `--temporal-aggregation`.
- **16 consecutive successful autonomous cube-twists.** The 07-22 strategy pivot is validated:
  more+cleaner data (197 eps, 178 clean) + no return-to-initial + temporal aggregation together fix
  the compounding/OOD drift that killed v0.
- **Generalization probe (cube slightly off the training pose):** policy **keeps retrying but makes
  no corrective adjustment** → EXPECTED — the demos contain no corrective behavior, so the policy has
  none to imitate (textbook BC-without-recovery). Concrete next-data signal: an off-pose-start +
  correction curriculum for spatial robustness.

**⚠️ Deploy command gotchas (this tree) — pinned down while running eval:**
- `--pose`/`--episode` are wired ONLY into the **gt-obs** path (`control_process.py:669` gates the
  start pose on `gt_obs_dataset is not None`; `--episode` only feeds `gt_obs_episode`). In **live**
  mode both are silently ignored → the robot does NOT auto-start at the task pose. **Live start =
  type `usepose rubik_twist_horizontal_v1` in the deploy window → `Tab` (ramp, place cube) →
  `Enter`.**
- **gt-obs** run (open-loop, actions from recorded obs): keep `--gt-obs <ds> --episode N --pose
  <name>`. Rule of thumb: **flags for gt-obs, interactive `usepose` for live.**
- Upstream-report add: make `--pose` apply to live deploy too (flag/interactive parity).

**🗓️ Timeline moved + week called here:**
- Demo shoot **postponed to Thu 07-30** (setup 07-29) → **07-27 + 07-28 = two days to finalize the
  pipeline + cut the stable demo branch.** The 07-25/26 crunch is relieved.
- No robot/glove state left mid-change; networks + XRobot service + gloves all up. Safe to resume.

**NEXT WEEK (07-27/28 — finalize):**
1. **Cut the stable demo branch** off the validated 07-20 tree + this week's edits (2 policy configs,
   `pico_world.yaml` head/torso hold, `wuji_finetune.json` recal, `repair_missing_images.py`);
   isolate from labmate churn; fix stale CHEATSHEET deploy commands. (07-23 item #3, still open.)
2. **Soften the reset grip** (transmission-slip mitigation, [issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md](../issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md))
   — baseline behavior now captured (16/16), so safe to change; keep the pose name so deploy-reset
   still matches the data.
3. **Camera frame-drops** still unfixed (batch1 ~8% / batch2 ~11%) — fix at source (USB3 5000M /
   `cameras.yaml` d455 60→30 fps / powered hub).
4. **Optional next-data:** off-pose + corrective-behavior curriculum to add recovery robustness.
5. 🛒 asset buy-list (now for 07-30); big upstream merge still DEFERRED post-demo.

### 2026-07-27

**On-site week begins. TIANJI staff on-site to redo the robot 走线 (cable routing). Post-rework
hardware verified via replay; then scoped the on-site demo provisioning.**

- **Post-走线 hardware check via replay** (`we data replay --pose rubik_twist_horizontal_v1`):
  arms + both hands + power/comms all verified end-to-end. Path to green was bumpy — gotchas logged:
  - Driver init-gate tripped (arms off-pose after the rework) → recovered via 上位机 (CHEATSHEET
    §7); driver then came up clean.
  - **Raw dataset can't be replayed** — `load_replay` loads every episode and rejects the whole
    set on the first faulty one → use the `_repaired`/`_merged` (all-clean) set. CHEATSHEET §4.1 added.
  - **Both gloves `WujiException: Connection timeout`.** Root cause = the two glove ethernet cables
    got **cross-wired** during the 走线 (each landed on the *other* USB dongle) — confirmed via
    tcpdump (dongle routed for `.100` answered from `.101` and vice-versa). Workstation net config
    was fine (static `.166/.167` + host routes, unchanged; a reboot doesn't move static IPs). Fix =
    swap the two cables. CHEATSHEET §3 troubleshooting note added.
  - Black MuJoCo window = forgot `unset __NV_PRIME_RENDER_OFFLOAD` (robo.nix re-exports it on every
    shell entry; must unset in the SAME interactive `robo shell`, not via `robo run`).
  - Replay profile: **`keyboard_wuji_hands`** drives both hands with NO PICO/XRobot dependency —
    simplest for replay (vs `pico_world_wuji_right`, which needs the xrobot bootstrap).
- **On-site demo provisioning scoped** (shoot 07-30, setup 07-29). Workstation is the only thing we
  can't bring (robot + Orin travel with us). 千兆网 but **NO VPN** → foreign caches unreliable
  regardless of local speed → plan = **offline SSD seed**. Teleop-only payload ≈ 13 GB (operator
  venv only; drop training venvs/policies; identical-repo-path trick avoids venv rebuild). Full
  runbook: [checklists/2026-07-29-onsite-provisioning-runbook.md](../checklists/2026-07-29-onsite-provisioning-runbook.md).
- **Coordination sent to TIANJI:** workstation specs + wifi + Wed-access questions (awaiting reply;
  the **NVIDIA-GPU** answer gates the MuJoCo viewer / graphics). **Quest whole-body:** chassis
  can't be teleoped (fails-closed in `gento.py` — needs a separate controller); torso WAS teleopable
  (our own `torso: hold` edit turned it off) → re-enable + operator-test. Headset choice (Quest vs
  PICO) is NOT the whole-body limiter — the receiver is headset-agnostic. Awaiting labmate confirm.

**07-28 (Tue) plan — heavy day, batched by operator-need:**
1. 🔴 **Nix→SSD migration rehearsal** (highest risk) — build the SSD, provision the spare
   workstation OFFLINE (network unplugged) per the runbook. Give it a real block.
2. **Operator session (batch):** (a) torso teleop re-check (sim first, then robot); (b) hand teleop
   A/B — prep TWO stable hand yamls (pinch-enhanced vs plain) + manual swap (`cp` preset + restart
   teleop; no switch mechanism exists).
3. **Hardware transport inventory** — packing list; adapter/hub count gated on TIANJI's port answer
   (pack generous: 2× USB-ethernet, USB hub, spare cables).
4. Chase replies: labmate (torso/head), TIANJI (workstation specs).
