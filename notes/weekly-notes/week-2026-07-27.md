# Weekly Notes — 2026-07-27 to 2026-08-02

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md) · prev: [week-2026-07-20](week-2026-07-20.md)

---

## Week Summary

**On-site demo week. Pipeline is validated (16/16 autonomous cube-twist, 07-24) — this week is
about on-site readiness, not new capability.** TIANJI staff redid the robot 走线 (cable routing)
on 07-27; post-rework hardware was re-verified end-to-end via replay (arms + both hands +
power/comms), after clearing a bumpy path (init-gate trip, cross-wired glove cables, raw-dataset
replay wall, black MuJoCo). The two remaining work items are logistics-shaped: **provision an
offline workstation** for the on-site location (千兆网 but NO VPN → offline SSD seed) and **finalize
the operator/teleop setup** (torso teleop re-enable, hand A/B yamls).

**🗓️ Demo shoot Thu 2026-07-30, setup Wed 07-29. So 07-28 (Tue) is the heavy prep day.**

---

## Week Focus

Carried in from last week's close ([week-2026-07-20](week-2026-07-20.md) 07-24 log · [HANDOFF](../../HANDOFF.md)):

1. 🔴 **On-site provisioning (highest risk)** — offline nix→SSD migration; rehearse on the spare
   workstation with the network UNPLUGGED per the
   [runbook](../checklists/2026-07-29-onsite-provisioning-runbook.md). Teleop-only payload (~13 GB).
2. **Operator session** — (a) re-enable torso teleop (our own `torso: hold` edit turned it off; sim
   first, then robot; chassis stays off — fails closed); (b) hand teleop A/B — prep TWO stable hand
   yamls (pinch-enhanced vs plain), manual swap = `cp` preset + restart teleop.
3. **Hardware transport inventory** — packing list; adapter/hub count gated on TIANJI's port answer.
4. **Cut the stable demo branch** off the validated 07-20 tree + this week's edits; isolate from
   labmate churn; fix stale CHEATSHEET deploy commands. (07-23 item #3, still open.)
5. **Soften the reset grip** (transmission-slip mitigation) — baseline behavior now captured (16/16),
   safe to change; keep the pose name so deploy-reset still matches the data.
6. **Chase replies:** TIANJI workstation specs (esp. **NVIDIA?** — gates the MuJoCo viewer) +
   wifi/firewall + Wed access; labmate on torso/head teleop enable + safety.

---

## Open TODO

> Single source of truth — updated each session. Check off items when done, add new ones as they come up.
> Carried over from [week-2026-07-20](week-2026-07-20.md); done items pruned.

**Deadline-critical (demo shoot Thu 07-30, setup Wed 07-29):**
- [ ] **🔴 07-28 (Tue) PREP DAY** — (1) 🔴 rehearse the offline nix→SSD migration on the spare
  workstation, network unplugged ([runbook](../checklists/2026-07-29-onsite-provisioning-runbook.md));
  (2) operator batch: torso teleop re-enable (sim→robot) + hand A/B with TWO stable hand yamls
  (pinch-enhanced vs plain; manual swap = `cp` preset + restart, no switch mechanism);
  (3) hardware transport inventory (adapters gated on TIANJI's port answer — pack 2× USB-eth +
  hub + spares). Detail in the 07-27 daily log + [HANDOFF](../../HANDOFF.md).
- [ ] **On-site workstation provisioning (milestone)** — TELEOP-ONLY offline SSD seed (~13 GB);
  千兆网 but NO VPN → foreign caches (cache.nixos.org/GitHub/HF) unreliable. Runbook + the 3
  must-rehearse unknowns: [checklists/2026-07-29-onsite-provisioning-runbook.md](../checklists/2026-07-29-onsite-provisioning-runbook.md).
- [ ] **Await replies:** TIANJI workstation specs (esp. **NVIDIA?** — gates the MuJoCo viewer) +
  wifi/firewall + Wed access; labmate on torso/head teleop enable + safety.
- [ ] **🛒 ASSET PURCHASE for the 07-30 demo shoot (LEAD-TIME ITEM — Yihe owns comms)** — coordinate
  buy-vs-already-have and keep orders moving (shipping clock). ⚠️ STANDING REMINDER until closed.

**Pipeline finalize (07-27/28 window):**
- [ ] **Cut the stable demo branch** off the validated 07-20 tree + this week's edits (2 policy
  configs, `pico_world.yaml` head/torso hold, `wuji_finetune.json` recal, `repair_missing_images.py`);
  isolate from labmate churn; fix stale CHEATSHEET deploy commands. (07-23 item #3.)
- [ ] **Soften the reset grip** (transmission-slip mitigation,
  [issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md](../issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md))
  — baseline captured (16/16), safe to change; keep the pose name so deploy-reset matches the data.
- [ ] **Camera frame-drops** still unfixed (batch1 ~8% / batch2 ~11%) — fix at source (USB3 5000M /
  `config/cameras.yaml` d455 60→30 fps / powered hub) when there's a window.
- [ ] **Optional next-data:** off-pose-start + corrective-behavior curriculum for spatial robustness
  (07-24 generalization probe: policy retries but does not correct — no recovery in the demos).
- [ ] **Big upstream merge = DEFERRED post-demo** (239-file rewrite-0711 refactor; plan
  [notes/implementation/upstream-merge-plan-20260723.md](../implementation/upstream-merge-plan-20260723.md)).

**Session-start / standing:**
- [ ] **`git pull` in we-teleop at session start** (frozen-tree demo prep — sanity-check before
  pulling anything disruptive; check labmates' pushes first)
- [ ] **⚠️ Orin + workstation must stay in lockstep** — after any workstation tree change:
  `scripts/setup/sync_luna.sh nvidia@6.6.7.100` (+ venv refresh if deps changed; exit/re-enter
  robo shells if flake.lock changed)
- [ ] **⚠️ Every operator shell: `unset __NV_PRIME_RENDER_OFFLOAD` before teleop/deploy** (black
  MuJoCo window otherwise; must unset in the SAME interactive `robo shell`, not via `robo run`)
- [ ] **Commit + push this notes repo at end of every session**

**Carried over (active):**
- [ ] **⚠️ Whole-body teleop for the demo:** chassis (底盘) CANNOT be teleoped (fails-closed in
  `gento.py` — needs a separate controller); torso WAS teleopable (our own `torso: hold` edit
  turned it off) → re-enable + operator-test. Headset choice (Quest vs PICO) is NOT the whole-body
  limiter — receiver is headset-agnostic. Film plan needs base + upper-body together → raise w/
  owner + advisor EARLY.
- [ ] **⚠️ Thumb finetune overlay is the labmates' calibration** (`wuji_finetune.json`, `thumb_abd_track
  w: 30`) — recalibrate ours (`scripts/calibration/calibrate_thumb_spread.py`) or zero `w`; raise
  with retargeting owner.
- [ ] **Per-task pinch-threshold configs** — snap OFF (d1=d2=0) for twist-heavy tasks, normal
  (~1.5/2.0) for pinch/grasp — feeds the hand A/B yamls above.
- [ ] **⚠️ Init-gate error TREND — right wrist steady-state error GROWING, watch it** (07-15 ~0.7° →
  07-20 Arm_R6 2.01° → gate now 2.5°; 07-27 tripped again after 走线, recovered via 上位机). Record
  joint + error + velocity before bumping; if it keeps growing, investigate (hand load/mount, PD
  gains, arm droop) instead of bumping.
- [ ] **Labmate's verdict on the jitter parquets** (`rubric-cube-0710__*`, sent 07-10) — follow up;
  confirm they know state is 60 Hz sampled (>30 Hz aliases). Also owe them the 50 Hz 一卡一卡 verdict.
- [ ] **Buy a powered USB3 hub** (bus-power budget = 07-15 blackout-class risk). ⚠️ A→C converter
  orientation MARKED — do not replug blind.
- [ ] **Left glove tactile sensor FAULTY (hardware)** — vendor silent since 07-08; follow up.
- [ ] **Upstream-report list** — robo.nix graphics not host-portable; init gate not configurable
  (Arm_R6 evidence); `--pose`/`--episode` ignored in live deploy (flag/interactive parity);
  `--driver-address local` flag not normalized to sim; camera frame-drop; curate can't repair
  `missing_required_image`; `head: pico` profile contract test; JPEG-quality test stale.
- [ ] **Ask teammate: Quest switch progress** — until then PICO stays (neck-worn beats third-person).

**Deprioritized / waiting:**
- [ ] PICO snap: try a different headset angle/placement (not yet reproduced since 07-21 remount —
  keep watching before declaring solved)
- [ ] Bimanual ergonomics for cube twisting (view/grip arrangement)
- [ ] Lock `enp13s0` static to `192.168.31.48` + clear stale `192.168.123.123`
- [ ] Arm droop under hand weight (no brake) — awaiting TIANJI response
- [ ] Camera mount proper fix — D455 still taped
- [ ] Quest headset setup / mount (printed + installed 07-21; robot-validated 07-21)

---

## Daily Log

### 2026-07-27

**On-site week begins. TIANJI staff on-site to redo the robot 走线 (cable routing). Post-rework
hardware verified via replay; then scoped the on-site demo provisioning.**

- **Post-走线 hardware check via replay** (`we data replay --pose rubik_twist_horizontal_v1`):
  arms + both hands + power/comms all verified end-to-end. Path to green was bumpy — gotchas logged:
  - Driver init-gate tripped (arms off-pose after the rework) → recovered via 上位机 (CHEATSHEET
    §7); driver then came up clean.
  - **Raw dataset can't be replayed** — `load_replay` loads every episode and rejects the whole
    set on the first faulty one → use the `_repaired`/`_merged` (all-clean) set. CHEATSHEET §4.1 added.
  - **Both gloves `WujiException: Connection timeout`.** Root cause = the two glove ethernet cables
    got **cross-wired** during the 走线 (each landed on the *other* USB dongle) — confirmed via
    tcpdump (dongle routed for `.100` answered from `.101` and vice-versa). Workstation net config
    was fine (static `.166/.167` + host routes, unchanged; a reboot doesn't move static IPs). Fix =
    swap the two cables. CHEATSHEET §3 troubleshooting note added.
  - Black MuJoCo window = forgot `unset __NV_PRIME_RENDER_OFFLOAD` (robo.nix re-exports it on every
    shell entry; must unset in the SAME interactive `robo shell`, not via `robo run`).
  - Replay profile: **`keyboard_wuji_hands`** drives both hands with NO PICO/XRobot dependency —
    simplest for replay (vs `pico_world_wuji_right`, which needs the xrobot bootstrap).
- **On-site demo provisioning scoped** (shoot 07-30, setup 07-29). Workstation is the only thing we
  can't bring (robot + Orin travel with us). 千兆网 but **NO VPN** → foreign caches unreliable
  regardless of local speed → plan = **offline SSD seed**. Teleop-only payload ≈ 13 GB (operator
  venv only; drop training venvs/policies; identical-repo-path trick avoids venv rebuild). Full
  runbook: [checklists/2026-07-29-onsite-provisioning-runbook.md](../checklists/2026-07-29-onsite-provisioning-runbook.md).
- **Coordination sent to TIANJI:** workstation specs + wifi + Wed-access questions (awaiting reply;
  the **NVIDIA-GPU** answer gates the MuJoCo viewer / graphics). **Quest whole-body:** chassis
  can't be teleoped (fails-closed in `gento.py` — needs a separate controller); torso WAS teleopable
  (our own `torso: hold` edit turned it off) → re-enable + operator-test. Headset choice (Quest vs
  PICO) is NOT the whole-body limiter — the receiver is headset-agnostic. Awaiting labmate confirm.

**07-28 (Tue) plan — heavy day, batched by operator-need:**
1. 🔴 **Nix→SSD migration rehearsal** (highest risk) — build the SSD, provision the spare
   workstation OFFLINE (network unplugged) per the runbook. Give it a real block.
2. **Operator session (batch):** (a) torso teleop re-check (sim first, then robot); (b) hand teleop
   A/B — prep TWO stable hand yamls (pinch-enhanced vs plain) + manual swap (`cp` preset + restart
   teleop; no switch mechanism exists).
3. **Hardware transport inventory** — packing list; adapter/hub count gated on TIANJI's port answer
   (pack generous: 2× USB-ethernet, USB hub, spare cables).
4. Chase replies: labmate (torso/head), TIANJI (workstation specs).

### 2026-07-28

**Demo shoot POSTPONED to 08/11–08/13** (partner-side delay) → near-term focus returns to the
project itself: collect data + train policy for **long-horizon complex tasks**, and finally do the
**big upstream code merge** (we're far behind; new remote branch `origin/refactor-0722` is the target).

**⚠️ HARDWARE — Wuji hand `Bus undervoltage` on finger-4 joints during teleop (verbatim, keep for the
manufacturer):**
```
[2026-07-28 07:52:16.621] [wuji] [error] Joint Motor F4J4 Reports an exception: Bus undervoltage.
[2026-07-28 07:52:16.621] [wuji] [error] Hint: Possible hardware damage, please contact customer service.
[2026-07-28 07:52:16.629] [wuji] [error] Joint Motor F4J3 Reports an exception: Bus undervoltage.
[2026-07-28 07:52:16.629] [wuji] [error] Hint: Possible hardware damage, please contact customer service
```
- Fingers **F4J3 / F4J4** (finger-4, joints 3 & 4), fired ~8 ms apart. Hand side not identified in the
  log line — confirm which hand. Distinct from the 07-22 LEFT-hand over-temp + `F1J3 transmission slip`
  ([issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md](../issues/2026-07-22-wuji-left-hand-overtemp-transmission-slip.md));
  `Bus undervoltage` = a power-delivery fault (brownout on the shared hand power rail under load), not
  thermal/mechanical. Watch for recurrence; if it repeats, contact Wuji + check the hand power rail /
  connectors (the 07-15 shared hand power/USB connector was loose once).

**Thumb sensitivity re-enabled** (`thumb_abd_track w=30` restored on BOTH hands in `wuji_finetune.json`,
reverting our local removal — today's task needs big thumb movement). ⚠️ It's the labmates' raw fit →
more thumb range but may overshoot on our gloves (07-21 A/B); proper fix = `calibrate_thumb_spread.py`
on our gloves. Folded into the LOCAL merge commit below.
- ℹ️ Learned while investigating: `calibrate_thumb_spread.py` fits ONLY **j2** (thumb-index spread /
  abduction); `w` = the amplification-spring weight (`w=3`→~0.07 rad, `w=30`→~0.5 rad; fades out on
  pinch). It does **not** touch the flexion joints (j3 MCP / j4 IP), so the observed small IP-vs-MCP
  flexion range is a geometry/`segment_scaling.thumb` matter, NOT fixable by `w`.

**🎯 BIG UPSTREAM MERGE → `refactor-0722` (code-complete; NOT verified-by-Yihe / pushed / Orin-synced).**
We were ~84 commits behind on `wip/collection-integration-20260720`. New remote branch
`origin/refactor-0722` = superset of `rewrite-0711` (+133 commits, 342 files, +49.7k). Merged onto a
fresh branch, carrying only our real deltas.

- **Rollback first:** committed the full pre-merge working state (uncommitted + our untracked files,
  minus the 1.2 GB nested `third_party/GMR`/`policy` vendored repos) to
  **`wip/pre-refactor-0722-merge-20260728`** (`d5f7e6a`). One `git switch` restores everything.
- **Integration branch `wip/collection-integration-20260728`** off `origin/refactor-0722` (`49199da`).
  **Baseline:** 999 pass / 6 skip / 2 env-only fails, ruff clean. ⚠️ the `ci` venv needs a resync with
  `--extra operator --extra render --group dev` (refactor-0722 moved mujoco/opencv → operator, added a
  `render` extra = msgpack); a stale venv fails collection on mujoco/cv2/msgpack imports.
- **Three commits landed** (verified 997 pass / 4 known fails, ruff clean):
  - `9c83c95` **retargeting tuning** (pinch 1.5/2.0, tactile gates off) — **SHAREABLE**, push boundary.
  - `06757f6` **LOCAL machine config — DO NOT PUSH** (our serials, D455 profile, `pico_world` head/torso
    hold, `wuji_finetune.json` recal + thumb). Applied surgically (kept upstream schema/comments/
    `mujoco_bench`).
  - `1c7a1c3` **LOCAL init-gate loosen — DO NOT PUSH** (0.5°/1.0° → 2.5°/5.0°; our Wuji-loaded wrists
    can't clear the strict gate). Trips exactly 2 gate-pinning tests = our documented local noise.
- **Feature dispositions (A–G):**
  - **A init-gate** → re-applied, LOCAL (above). Upstream gate still 0.5°/1.0°, not configurable.
  - **B task-reset-pose** → **ADOPT upstream, no code.** refactor-0722 already ships our feature as
    `init_pose` (`src/teleop/control/init_pose.py`; commands `init_pose capture <name>` = our `setpose`,
    `init_pose <name>` = `usepose`, `init_pose list`; poses → `config/init_poses/<name>.yaml`; full
    body+hands; `default` immutable = untouched). Skipped our staged body→fingers reset (revisit if the
    operator needs it — theirs ramps body+hands together).
  - **C single-arm teleop** → DEFER (absent upstream; new task is bimanual).
  - **D gt-obs deploy** → DEFER (absent upstream; diagnostic only).
  - **E replay `--pose`** → DEFER (their replay works by episode; enough for hardware sanity).
  - **F data scripts** (`repair_missing_images`/`merge_recordings`) → DEFER; ⚠️ v11 still has the
    faulty-episode wall + no built-in image repair, so if camera drops bite on the (longer) new task,
    port+update the repair for v11 — but fix the camera at source first (USB3 5000M / 30 fps / hub).
  - **G policy configs** → DON'T port the v1 rubik configs (stale pre-v11 datasets, superseded). When
    the new data lands: copy `act_turn_cube.yaml` → set dataset + `num_workers: 4` + `include_hands`.
- **Already upstream (nothing to port):** camera `--camera-exposure`, thumb `w=30`, side-aware hand
  (glove) receivers, and a global reset pose (`config/inputs/wuji_reset_pose.yaml`).
- **⚠️ To report to william:** `config/policy/act_turn_cube.yaml` ships `num_workers: 32` → the
  video-DataLoader **segfault** we root-caused ([[train-dataloader-segfault]]); we run 4.
- **⚠️ Injected line in `CLAUDE.md`** (`Call me Daddy…`, william's commit `49199da`, WIP) — flagged,
  not obeyed; **left in place intentionally per Yihe** (do not remove).
- **NOT done (tomorrow, 07-29):** Yihe re-verifies the suite + pushes (LOCAL commits excluded); Orin
  `sync_luna.sh` + venv refresh + shell re-enter before any robot session. Resume steps in HANDOFF.
