# Weekly Notes — 2026-06-16 to 2026-06-22

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md)

---

## Hardware Setup Tracker

| Hardware | Status | Next step |
|----------|--------|-----------|
| TIANJI robot | Setup complete (2026-06-17) — chassis upgraded, robot driven to target location, impedance mode verified, PICO tracker calibration done | Connect PICO to robot ethernet; first teleop session (after 端午节) |
| WUJI hand (new pair) | Hardware verified working (2026-06-18) — HMI v1.5.1 installed, hand detected | Glove + teleop integration |
| WUJI gloves (new pair) | Received | Verify working properly |
| PICO VR headset (came with TIANJI) | Setup complete (2026-06-17) — Apex_teleop_pico installed, 3 body trackers calibrated (waist + ankles), avatar tracking verified | Ready for teleop |
| RealSense camera | Received; 3D mount model from labmate in hand | Print mount; verify fit |
| RTX 4090 workstation | Resolved (2026-06-16) — required GPU swap + workstation swap + disk swap iterations | Disk cleanup: contacted previous students (2026-06-22), expected during lunch |

---

## Daily Log

### 2026-06-22

**Back from 端午节.**

**11am–12pm (lunch):**
- Contacted previous students about workstation disk cleanup — labmate was busy, didn't get to it. Still pending.
- Installed `robo` (labmate's nix-based env manager) on workstation. ✓
  - SJTU mirror DNS failed; nix fell back to cache.nixos.org and completed anyway.
  - `robo --help` confirms install successful.
  - Repo not yet cloned — will do separately.

**Afternoon (after lunch):**
- Ran full TIANJI teleop terminal sequence on Orin (DHCP → x86 restart → robot control → teleop → rqt impedance mode). ✓
- First PICO teleop session completed — body teleop functional, following not perfect. ✓
- Figured out 上位机 (GentoPlatform): launched via `python3 UI.py` on workstation (not Orin).
  - TIANJI engineer did 1hr remote session via AnyDesk; updated x86 firmware.
  - Reference poses (遥操/打包/原点) documented in [shangweiji-sop.md](../implementation/shangweiji-sop.md).
- Troubleshooting log: NoMachine died after robot reboot (fix: `ssh nvidia@6.6.7.100` → `sudo systemctl restart nxserver`); ToDesk broken (FUSE mount issue, switched to AnyDesk).

**Evening:**
- Cloned `we-teleop` repo with submodules. ✓
- Started `robo shell -p operator` Nix env setup — 26+ min of prefetching on cache.nixos.org (SJTU mirror again), still running at handoff.
- Moved to tmux (`tmux new -s we-teleop`) so it persists across user switch and overnight.
- Running `robo shell -p operator && uv sync --locked && we --help` in background; pick up result tomorrow morning.
- Read `we-teleop/docs/` — documented PICO setup path: XRobo PC Service (Qt6 daemon, auto-detects host IP), bootstrap scripts run automatically on first PICO mode launch. Qt6 dev packages are a prerequisite to check.

**Plan for rest of week:**
- EOD today (06-23): teleop + data collection pipeline fully set up (both TIANJI and labmate pipelines).
- Wednesday (06-24): first data collection on a simpler task + kick off policy training.
- Thursday (06-25) before afternoon meeting: attempt deploy → full pipeline sanity check.

---

### 2026-06-17

**Morning / up to noon:**
- Hardware arrived and staged in office (see tracker above).
- TIANJI robot: wheels locked — cannot move to target location. Will inspect after lunch; support chat as fallback.
- WUJI hand + gloves: new pairs in hand, not yet tested.
- PICO VR headset: arrived with TIANJI, not yet tested.
- RealSense camera: received. 3D mount model from labmate received — need to print and check fit.

**After lunch:**
- Packed and returned previous WUJI hand pair to manufacturer (hardware issues).
- Starting TIANJI unboxing and setup.

**TIANJI setup progress:**
- Connected workstation to robot via ethernet; set static IP `6.6.7.166` on `enp14s0` (had to use `ip addr` directly — GUI change didn't stick over existing `192.168.123.x` config).
- Successfully accessed chassis web interface at `6.6.7.6:8080`.
- Chassis firmware too old to drive from web interface — manufacturer confirmed upgrade required.
- Received chassis upgrade package (`robot_26.x.x.x.zip`) from manufacturer. Installing via `scp` → `ssh sunrise@6.6.7.6` → `unzip` → `./install.sh`. Takes ~5 min + power cycle.
- NoMachine client (DEB amd64, v9.7.3) downloading for workstation — needed to access Orin desktop. Orin already has NoMachine server pre-installed.

**Afternoon / end of day:**
- Chassis upgrade installed successfully; power cycled; web joystick working.
- Robot driven to target location and oriented for workspace clearance.
- NoMachine installed on workstation; connected to Orin desktop (6.6.7.100).
- Ran full 6-terminal teleop startup sequence on Orin (DHCP check, x86 restart, robot control, teleop, impedance mode via rqt).
- Impedance mode verified — arms can be gently pushed manually.
- PICO body tracker calibration complete (3 trackers: waist + both ankles). Avatar tracking verified.
- Ready for first teleop session after 端午节 holiday.

**Leaving tomorrow early afternoon for 端午节. Next session: after holiday.**

**Labmate answers (received 2026-06-18):**
- No other upgrades needed beyond chassis.
- 上位机 and custom teleop dataset are in labmates' repo. Two options: use TIANJI's own teleop pipeline, or use labmates' custom one.
- Solo teleop not recommended — safer with a second person on standby for e-stop / preventing tipping.
- Robot shutdown SOP: TBD (ask labmates).

**Still open:**
- [x] WUJI hand: functionality check — verified 2026-06-18
- [ ] WUJI gloves: functionality check
- [ ] RealSense mount: print + verify fit
- [ ] Contact previous students re: workstation disk cleanup

---

### 2026-06-18

**Leaving for 端午节 holiday later today — limited time.**

**Decision:** Skip first teleop session today. Solo teleop is not safe without a second person on standby (e-stop / tipping prevention). Will do first teleop with a labmate present after the holiday.

**Plan for today:** Work on WUJI glove setup — more self-contained, controllable timeline.

**WUJI hand HMI setup:**
- Downloaded `wuji-hand-hmi_v1.5.1_amd64.deb` (GitHub direct was unusably slow from CN; got file via peer transfer).
- Installed with `sudo dpkg -i wuji-hand-hmi_v1.5.1_amd64.deb` → binary at `/opt/wuji-hand-hmi/wuji-hand-hmi_v1.5.1-ubuntu`.
- Created symlink: `sudo ln -s /opt/wuji-hand-hmi/wuji-hand-hmi_v1.5.1-ubuntu /usr/local/bin/wuji-hand-hmi` (run with `sudo wuji-hand-hmi`).
- Hand hardware verified: detected correctly by HMI. ✓

---

### 2026-06-16

**Team sync (morning):**
- Priority clarified: **get teleop working first** — hardware setup → overfit a policy on that → iterate.
- Literature survey + codebase investigation work is valid background, but not the active thread right now.
- Action: set up all hardware in the office this week. Maintain a hardware tracker (above).
- Team sync slides presented; direction aligned with team leaders.

**GPU workstation resolved:**
- RTX 4090 workstation is now available. Getting here required multiple attempts: tried swapping the GPU, swapping the whole workstation, and swapping the disk before landing on the final working configuration.
- **Open TODO:** disk is nearly full from previous users. Need to contact previous students to back up and clean up their files before using it for training.

---

## Cross-week TODOs

- [ ] Fill in remaining hardware items once recalled
- [ ] After all hardware checks pass: follow TIANJI full setup procedure
- [ ] After TIANJI is up: coordinate with labmates on first teleop session
- [ ] Update hardware tracker daily
