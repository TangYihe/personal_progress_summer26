# Weekly Notes — 2026-07-06 to 2026-07-12

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md)

---

## Week Summary

*(written 07-10 — last session of the week; 4-day break after, back ~07-15)*

**Big picture: the Rubik's-cube teleop went from "barely possible" to "a lot better" over the week**, via ergonomics (PICO rack, lock-torso, operator view robot-right/operator-left ✓ verified 07-10, elbow support, operator training) + retargeting fixes (colleague's tuned config on master + our middle-finger `d2` 3.0 tweak).

**Key milestones:**
- **Switched to `master`** (07-08, from `feat/multiprocessing`) — Jiajun's calibration rework + colleague's per-finger retargeting config (`346fa59`). New `calibrate_wuji_hand.py` run for both hands → finetune store overlay (currently `.off` — Test B pending).
- **Camera all-white bug root-caused + fixed** (07-09): exposure-lock UNITS mismatch — D405 µs vs D455 100µs-units → `D405_EXPOSURE_US=100` local fix, verified on robot; safety branch `wip/d455-exposure-fix-20260709` (`96cc236`). Upstream `--camera-exposure` flag still TODO.
- **Jitter/一卡一卡 data captured WITH camera** (07-10, `rubric-cube-0710__*`, 6 eps) + **parquets sent to labmate**. Working theory (labmate): PICO reading issue; our rack-tilt/occlusion hypothesis + stationarity A/B still untested.
- **Pinch over-sensitivity saga closed**: root cause was the old calibration script's degenerate thresholds (07-07), script since deleted upstream; on colleague's config pinch feels good in sim.
- **Glove extension cable attempt failed** (07-10): passive USB3 extension → `-EPROTO` storms; running without. Left glove hung during re-cabling → power-cycle recipe documented.
- **Hardware status**: left glove tactile sensor confirmed faulty (vendor contacted, no reply); PICO rack sleep bug unresolved (nudge workaround).

**Carried to next week:** PICO stationarity A/B; Test B finetune-store; on-robot feel of new retargeting; labmate's jitter-data verdict; torso pose lower; PICO sleep; vendor follow-up; `we data rerun` viewer freeze.

---

## Open TODO

> Single source of truth — updated each session. Check off items when done, add new ones as they come up.

**Reminders:**
- [x] Ask Chen to send the operator — done, operator trained 2026-07-07 afternoon
- [x] Make + maintain a list of borrowed items — done 2026-07-07 *(TODO: link/location of the list)*

**New (from 2026-07-10 session):**
- [ ] **Glove cable extension — passive USB3 extension DOESN'T work** (see 07-10 log): caused `-EPROTO` error storms + disconnects on the right glove's RTL8153 adapter. Running WITHOUT extension for now. When reach is needed again: **active/repeater USB extension or powered hub**, or better — **extend on the ethernet side** (longer RJ45 adapter↔glove; ethernet tolerates length, USB3 doesn't)

**New (from 2026-07-09 session) — handle in order 1→3→2 before checking off today:**
- [ ] **1. Lighting / fixed exposure** — master driver locks exposure (AE off, `D405_EXPOSURE_US`, Xiawei `296574d` 07-05) → our D455 all white. ROOT CAUSE = **UNITS mismatch**: D405 exposure is in µs, but D455 RGB sensor takes 100µs units → their `10000` = 10ms on D405 but 1 SECOND on D455 (3000 = 300ms, still white — confirmed). Local fix: `driver.py` value → `100` (=10ms; step of 10 ≈ 1ms, tune 60–160 watching `d405_stream_client.py`). If grainy/odd at sane exposure: gain is frozen wherever AE left it — pin `rs.option.gain` too. **Flag to labmates: constant not portable across cameras, needs per-camera config.**
- [x] **2. Operator view fix** — done 07-09 evening: moved the ROBOT to the right (equivalent to seat right→left); **07-10 verdict: WORKS — cube teleop a lot better with the new view** ✓
- [ ] **3. Stutter/jitter = PICO reading issue (per labmate)** — 机器有时一卡一卡 / 人没动机器开始抖动 (seen today + previously); labmate says it's the PICO readings, not controller/gains. **Insight 07-09: controller tracking = the FOUR corner mono cams (two front-center = RGB passthrough, unused); our rack TILT means mostly only the lower two see the controllers → less redundancy (occlusion dropouts → IMU drift → snap = 卡顿) + weaker SLAM (upper cams at ceiling).** Next: 10-min stationarity A/B — sim-only `we teleop --profile pico_ee_absolute`, controllers resting on table, tilted vs straightened; whichever shows calmer frozen hands wins. Also suspect rack wobble from the anti-sleep nudges. Ask labmate what exactly "PICO reading issue" means (tracking vs data path).
- [~] **Ask Eden for more teleop tasks / objects** — picking up items 07-09 evening

**New (from 2026-07-08 pm session):**
- [x] **⏺ Arm jitter with STATIONARY controller (seen during 07-08 teleop)** — RESOLVED the data question 07-10: re-recorded WITH camera (`rubric-cube-0710__20260710-105655`, 6 eps) + **parquets sent to labmate** — awaiting their analysis. ⚠️ Follow up: make sure they know state is 60 Hz sampled (jitter >30 Hz aliases). (07-09 camera-less recordings `teleop-bug1..3` + `lagging_movement.zip` now redundant.)
- [ ] **Left glove tactile sensor FAULTY (hardware, confirmed)** — vendor contacted 07-08, awaiting reply on replacement. Also closes the 07-07 "left tactile reads ~zero" mystery: it was the sensor all along, not calibration/config
- [~] **Test the new hand calibration script / retargeting alg on master** — calibration DONE for both hands 07-08 pm ✓ (`calibrate_wuji_hand.py` → `~/.we-teleop/wuji_finetune.json`; distance thresholds only, pressure fits skipped on both hands). Running the new retargeting alg: in communication with the retargeting colleague — pending
- [ ] **Right pinky pinch threshold anomaly** — calibration wrote `d1: 7.95 / d2: 9.95` cm (other fingers 1.7–2.6) → very loose distance gate; if right pinky mis-triggers, re-run `calibrate_wuji_hand.py --hand right --pinch-fingers pinky`
- [x] ~~Glove readings extremely weird (suspected magnetic interference)~~ — recovered later same day; calibration ran fine afterwards
- [ ] **Adjust default torso pose LOWER** — operator wants a higher view relative to the robot but no higher chair is available; lower the robot's default torso pose instead
- [x] ~~Find props + tasks for tomorrow's (2026-07-09) teleop~~ — session done; follow-on: ask Eden (07-09 item above)

**New (from 2026-07-07 session):**
- [ ] **Bimanual ergonomics for cube twisting** — to see the right hand's working fingers, the left hand must hold a very tiring pose; figure out a better view/grip arrangement
- [ ] **PICO dims/sleeps every 5–10 min on rack** — NOT network; psensor IS fooled by tape; **07-08 evening: adb settings (`stay_on_while_plugged_in=7` + `screen_off_timeout=max`) + USB-C power ALSO failed — still drops.** Supports the "too-static reading" hypothesis. **Interim workaround: physically nudge/move the headset every few minutes during sessions.** Next diagnostic: `adb shell dumpsys power | grep -iE "wakefulness|timeout|sleep"` right after a drop
- [x] ~~Pinch threshold tuning~~ — **SUPERSEDED 2026-07-08 pm: switched to `master`** (labmates' suggestion); our tuning edits deliberately NOT ported (archived on `wip/pre-master-switch-20260708`) — testing their stock code + new calibration first

**This week's focus:**
- [x] Pull updated hand teleop code — done 2026-07-06: on `feat/multiprocessing` (hand code lives there ONLY); local changes snapshotted on `wip/pre-multiproc-20260706`
- [x] Re-test pinch over-sensitivity in sim on new retargeting — PASSED 2026-07-06, no sticky/over-sensitive pinch
- [~] Rubik's cube teleop trial — first dual-hand attempt 2026-07-06; task still hard → ergonomics improved (lock-torso, rack, view/elbow) + operator training from 2026-07-07 afternoon

**Teleop quality (carried over):**
- [ ] PICO wrist calibration — redo once mounts are stable
- [~] Right-hand pinch over-sensitive — reproduced in sim 2026-07-03; colleague's new retargeting on `feat/multiprocessing` — re-test this week

**Hardware (carried over):**
- [ ] Left Wuji glove dead — replacement requested; #1 dual-hand blocker
- [ ] Arm droop under hand weight (no brake on end motors) — reported to TIANJI, awaiting response
- [ ] Camera mount proper fix — D455 still taped
- [ ] Quest headset setup
- [ ] 3D scan robot head structure — scan with iPhone, send to 3D-modeling owner

**Software / infra (carried over):**
- [ ] `we data rerun` viewer — 07-10: native viewer fails (xlib/winit load error, egl-wayland didn't fix); browser mode works but freezes ~10 s into a full episode. Try `--max-frames` windows or `--save .rrd` + standalone viewer; or fix xlib libs in robo.nix
- [ ] Lock `enp13s0` static to `192.168.31.48` + clear stale `192.168.123.123`
- [ ] Background board — ask W or S
- [ ] Verify hand opens on `Tab` reset in new deploy (hand-reset fix not re-applied upstream)

---

## Daily Log

### 2026-07-06

**Goal (morning, before noon meeting):** pull updated hand teleop code and try the Rubik's cube teleop task on it.

**Plan:**
1. Snapshot local changes (`wip/pre-multiproc-20260706`), then switch to `feat/multiprocessing` — hand retargeting code lives there ONLY (not master, contrary to Friday's expectation).
2. Restore local files selectively; hold back pinch yamls (test new retargeting clean first) and driver.py gain overrides (reapply only if needed).
3. Sim re-test of pinch: `sim_glove_to_hand.py --side right`.
4. `sync_luna.sh` Orin to the same branch; driver smoke test (`--control-mode pd`).
5. Rubik's cube teleop trial.

**What was done:**

- **Switched to `feat/multiprocessing`** ✓ — hand retargeting code lives there ONLY (per hand-code owner), not master. Local changes snapshotted on `wip/pre-multiproc-20260706`; policy configs + diagnostics script restored; pinch yamls + driver.py gain overrides deliberately held back (old calib targets the old retargeter). Branch vs master: branch adds new retargeting (v1 optimizer, `83b422e`/`98daf5f`) + multiprocessing + reset fixes; master has 8 camera/recording commits NOT on the branch (60→90fps, extrapolation — matters for collection later, not for teleop).
- **New retargeting sim test PASSED** ✓ — `sim_glove_to_hand.py --side right`: no sticky/over-sensitive pinch. Kinematic mode survived on the branch (colleague built `--hand-model` on top of it; `assets` default = same hand as before).
- **Going dual-hand from today** — CHEATSHEET updated: both serials exported, `--wuji-hand-side both`, pinch calib for both hands. Teleop profile already bimanual (`left_hand/right_hand: true`).
- **Left hand serial found**: `366239543134` (via `lsusb -d 0483:2000 -v` on Orin; right = `306735523434`). Hands are USB `0483:2000`; driver USB-resets + retries init by default (transient "configuration timed out" self-heals).
- **Left glove ALIVE** at `192.168.1.100` — 07-02 dead-link was the adapter/cable chain, not the glove. (TODO: note which adapter is in use now.)
- **Orin synced to the branch** via `sync_luna.sh nvidia@6.6.7.100` (working-tree mirror).

- **PICO reconnected after charge** — same 07-02 failure signature: XRobot service not started + stale WiFi IP in `setting.ini`; fixed with the standard `TELEOP_XROBOT_HOST_IP=192.168.31.48` + re-source. (Network itself was fine.)
- **上位机 launch added to CHEATSHEET (§7)** — `~/Tianji/人形控制器版本及SDK_00040403_260604/SDK/00040400/FX_PLATFORM` + `python3 UI.py`; full SOP in [implementation/shangweiji-sop.md](../implementation/shangweiji-sop.md).
- **🎯 First dual-hand Rubik's cube teleop attempt (morning)** — full stack ran end-to-end: both hands + both gloves + PICO on `feat/multiprocessing`. **Task itself still hard** — more attempts in the afternoon.

**Noon meeting — team suggestions for the cube task:**
1. **Lock torso** for stability → driver already supports it: `--lock-torso` (torso holds init pose in PD, teleop torso input ignored; `driver.py:2043`). Added to CHEATSHEET §1 as the default setup.
2. Operator view: stand slightly 斜前方 of the robot for the best viewpoint.
3. Elbow support for the operator — 不能手臂悬空 (arm fatigue wrecks precision).
4. Operator practice time matters — schedule reps.

(For 2+3: consulting the experienced-teleop colleague later this afternoon for a proper setup.)

**Afternoon (revised):** building a **rack to hang the PICO headset** so the operator doesn't have to wear it during teleop (hardware acquired). Ergonomics follow-ups (view angle, elbow support) with experienced colleague.

**PICO rack built ✓ + racked-headset gotchas root-caused (evening):**
- Rack done — headset hangs instead of being worn; operator gets direct view. Only controller poses are used (no human model), so off-head is fine in principle.
- **"Tracking stopped" / `CANCELLED, uninitialize sdk` stream drops = headset SLEEPING on the rack** (proximity sensor uncovered → suspend after screen timeout → stream dies). Wearing it = sensor covered = awake — why mornings looked fine and "reconnects on pickup". **Fix: tape over the proximity sensor + set screen timeout/auto-sleep to never.**
- **`IK difference too big` (auto-pause) when moving the headset = jump guard, by design** — headset motion shifts the SLAM world frame, all controller poses jump, code rejects + pauses (`controller.py reject_large_jump`). Recovery: `Enter` re-anchors (jump-free). Rule: park headset before session; if moved → `Space`, move, settle, `Enter`.
- Left/right NOT flipped by rack placement: anchor math is translation-only; direction mapping follows the PICO world-frame yaw (set at last recenter), not live headset pose. If directions come out 180° rotated: recenter (long-press home) with headset facing the operator's direction, re-anchor. Sim-only test: `we teleop --profile pico_ee_absolute` (no `--driver-address` = pure sim, no gloves needed).
- PICO wired via USB-C adapter got `.110` (was `.231` WiFi); IP is irrelevant to config (PICO dials WS `.48`). Wired adapter seemed flaky but may have been sleep cycles in disguise — re-judge with sensor taped. On WiFi for now.

- **✅ Sleep fix VERIFIED in sim (end of day)** — tape + no-sleep: racked-headset teleop stable, no stream drops. Called it a day here.

**Tomorrow (2026-07-07):**
- *Morning:* deeper check of the new hand retargeting alg (sim passed Fri's symptom check; poke edge cases / on-robot feel). **On arrival: ask Chen to send the operator.**
- *Afternoon:* operator comes in — train them on different tasks (cube + others). Setup ready: PICO rack (+ tape + no-sleep), `--lock-torso`, 斜前方 viewpoint, elbow support.

### 2026-07-07

**Morning (light):**
- Borrowed-items list made ✓ (maintain going forward).
- Deeper retargeting check NOT done in the morning — deferred to the afternoon session (test on-robot feel alongside operator training).

**Afternoon plan:** operator training (cube + other tasks) + on-robot check of the new retargeting. Full teleop pipeline run with the new setup: dual-hand + `--lock-torso`, racked PICO (tape + no-sleep), 斜前方 view, elbow support.

**Right-pinch over-sensitivity RETURNED → root-caused to today's calibration, NOT code:**
- `calibrate_wuji_pinch.py` runs (13:15 L / 13:24 R) wrote degenerate thresholds: right `p_on 0.083 / p_full 0.092` (0.009-wide band ≈ hair trigger; rest floor ~0.055 vs weakest firm pinch ~0.115) + loosened distance gates (3.9/5.9 vs default 2.5/4.5). Left even worse: `p_full 0.001` = firm pinches recorded ~zero pressure → press-pinch effectively DEAD on left (why left "felt fine").
- **Reverted both yamls to branch defaults (`git checkout --`) → much better. Confirmed.**
- Open Qs: why calibration saw hot rest floor + weak firm pinch on right, ~zero tactile on left — glove fit / wearer hand size (operator?) vs. tactile hardware. New diag tool: `scripts/diagnostics/monitor_wuji_tactile.py --hand right|left` (live per-taxel grid + pair pressures). Left tactile ~0 would also mean press-pinch never fires for left teleop — check before relying on it.
- Rule of thumb: after any pinch calibration, sanity-check printed thresholds (`p_on` well above floor, `p_full` well above `p_on`; defaults 0.15/0.35 for scale) before trusting it.

**Afternoon — operator training (end-of-day summary, operator off at ~17:00):**
- ✅ **Good progress on single steps of the Rubik's-cube twist task.**
- ⚠️ **Bimanual coordination is very hard** — core issue is ergonomic: the right hand does the twisting, but to actually SEE both working fingers, the left hand has to hold a very tiring pose. Needs a rethink (view angle / grip role split) — carried to TODO.
- **Retargeting verdict:** over-sensitivity = calibration script's doing (pinpointed, see above). On branch defaults the pinch slightly UNDER-triggers instead — `d_near_cm: 2.5` a bit small; plan: bump to 3.0 and re-validate.
- **PICO still dims/sleeps every 5–10 min** even with white paper taped over the psensor. Confirmed NOT network: wake needs the physical power button (startup view shown) while the xrobot connection stays alive throughout.

**Evening — PICO psensor investigation:**
- Printed psensor status live: **the tape DOES fool the sensor** (reads "worn"). So the sleep isn't a simple off-head timeout. Working hypothesis: the reading being perfectly static (no micro-variation a real head gives) may make the system dim/suspend anyway. To continue.

### 2026-07-08

**Morning — pinch threshold tuning (helped a little, NOT enough — continue pm):**
- Threshold progression on `d_near_cm`: 2.5 (default) → 3.0 both hands → right per-finger `{index: 3.5, rest 3.0}` → left scalar 4.0. Verdict: **small improvement only**; more tuning / deeper investigation needed this afternoon.
- **Per-finger `d_near_cm`/`d_far_cm` support ADDED locally** to the vendored optimizer (`third_party/.../opt/enhanced_analytical.py` — `_parse_per_finger`, uncommitted). ⚠️ **Smoke test never ran** (blocked mid-session) — right-hand dict config is live but UNVERIFIED; if teleop logs show retarget errors, suspect this first. Revert: `git checkout -- third_party/wuji_retargeting_v2_pkg/wuji_retargeting_v2/opt/enhanced_analytical.py` + set right yaml `d_near_cm` back to scalar.
- Per-finger yaml gotchas: unlisted fingers fall back to code default **2.0** (not siblings) — always list all four; `calibrate_wuji_pinch.py` regex can't write into dict values (fails loudly, fine).
- Left `d_far_cm` still 4.5 → with `d_near 4.0` the distance ramp is only 0.5 cm (near-binary gate). If left feels all-or-nothing, widen `d_far` to ~5.5. Left tactile still unexplained (calibration saw ~zero pressure) — if left pinch never fires regardless of distance, it's the PRESSURE side: `monitor_wuji_tactile.py --hand left`.
- **PICO stay-awake set via adb** ✓ (verified applied): `stay_on_while_plugged_in=7` + `screen_off_timeout=max`. Needs USB-C power on the rack to guarantee no sleep. **10-min rack survival test still pending.** If it still dims: `adb shell dumpsys power | grep -iE "wakefulness|timeout|sleep"` right after.
- Workstation was rebooted 07-07 → re-checked XRobot flow; note `enp13s0` is still DHCP (static-IP TODO still open) — check `ip addr show enp13s0` = `.48` after any reboot.
- CHEATSHEET updated: §3 calibration warning + tactile monitor, §3b sim visualization commands, §4 racked-PICO rules.

**Afternoon plan:** more pinch tuning/investigation (threshold sweep in sim via §3b before robot time; distinguish distance-gate vs pressure-gate vs tactile-hardware causes), operator task exploration, PICO 10-min rack test.

**Afternoon — SWITCHED TO MASTER (labmates' suggestion); pinch-tuning thread shelved:**
- Workstation now on `master` @ `157251a` (`fix: hand retarget calibration` — Jiajun's rework: DELETES `calibrate_wuji_pinch.py`, the script whose degenerate output caused the 07-07 over-sensitivity; adds `scripts/calibration/calibrate_wuji_hand.py`, a per-hand finetune-store overlay in `session.py`, tests, `docs/wuji.md`).
- **Our 3 pinch-tuning edits deliberately NOT ported** — master's `adaptive_analytical.py` parses `d_near_cm` as scalar only (per-finger dict would crash), and our values were tuned against the branch retargeter anyway. Plan: evaluate their stock code first. Everything (yamls, per-finger py edit, both diagnostic scripts, .baks) archived on **`wip/pre-master-switch-20260708`**; only the 4 `act_pick_toy*.yaml` policy configs restored (staged).
- Master pinch note: `pinch_pressure.enabled: false` by default — opt-in after running the new calibration; session logs which calibration overlay applied at startup.
- Branch bookkeeping: retargeting commits (`83b422e` etc.) still NOT on master — they also live on active `feat/wuji-hand-v2`; 11 multiprocessing-branch commits not on master. Ask labmates about the merge plan.
- Sim smoke on master PASSED (`sim_glove_to_hand.py --demo --no-viewer` in the operator profile) — env fine after the switch.
- **Real glove sim test: readings extremely weird — suspect magnetic interference.** Put aside, retest later.
- ⚠️ **Orin NOT synced yet** — still on the old branch tree; `sync_luna.sh` before any robot session.

**New TODOs captured (see Open TODO):** test new calibration/retargeting; lower default torso pose (operator higher view, no higher chair); pick props + tasks for 2026-07-09 teleop.

**Late afternoon — gloves recovered; NEW calibration run for BOTH hands ✓:**
- Glove readings back to normal (earlier weirdness transient). Ran `calibrate_wuji_hand.py` → `~/.we-teleop/wuji_finetune.json` now has left + right blocks (segment scaling, pinky joint-tracking, per-finger pinch distance thresholds). Base yamls stay pristine; sessions overlay it automatically (`applied fine-tune overrides` in startup log; delete/rename the JSON to fall back to stock).
- **Pressure fits skipped on BOTH hands** (store has distance `d1/d2` only, no `p_on/p_full`) — `pinch_pressure` stays `enabled: false`, so no functional impact for now.
- **Left glove tactile sensor CONFIRMED FAULTY (hardware)** — explains the skipped pressure fit AND the 07-07 "left tactile ~zero" calibration mystery. → Contact the responsible colleague for a replacement.
- ⚠️ Right pinky pinch fit suspicious: `d1 7.95 / d2 9.95` cm vs 1.7–2.6 on other fingers (pinky pinch pose probably didn't connect) — re-run `--hand right --pinch-fingers pinky` if it mis-triggers.
- New retargeting alg: in communication with the retargeting colleague about how to run it — pending their input.

**Evening — colleague's retargeting config + code now on master; ready to test tomorrow am:**
- Colleague sent their tuned right-hand config → quick test on master confirmed **incompatible** (per-finger `w_hyper`/`soft_min` dicts vs scalar-only vendored optimizer; `TypeError: dict - float` on first retarget frame). Reported → **colleague pushed `346fa59 fix: retarget config` to master** (per-finger optimizer + their tuned configs). Pulled ✓ — `config/inputs/retargeting/wuji_right.yaml` is now byte-identical to the file he sent; nothing to copy manually.
- Overlay compat checked: finetune store format unchanged (only `press_grip` dropped from overlayable keys; our store has neither). ⚠️ **Store currently DISABLED** (`~/.we-teleop/wuji_finetune.json.off`) so the next sim run = pure colleague config (Test A).
- **Colleague's debugging guidance:**
  1. Fingers snapping together too eagerly → lower that finger's `d2` (far distance threshold) in `pinch_thresholds`.
  2. Tactile pinch enforcement uses **min(thumb taxel, finger taxel)** — both finger PADS must press together for the pressure gate to fire. Our earlier pinches sometimes didn't do this → likely why tactile assist felt absent.
- Left glove tactile: **vendor contacted** about the faulty sensor (was: contact responsible colleague).

**Tomorrow (2026-07-09) morning:**
1. Sim A/B: Test A = pure colleague config (store off, current state); Test B = `mv ~/.we-teleop/wuji_finetune.json.off ~/.we-teleop/wuji_finetune.json` + same command (watch `applied fine-tune overrides` in log). Pinch with PADS pressed together. If a finger over-sticks → lower its `d2`. Watch our right-pinky `d1 7.95` overlay in Test B.
2. Adjust default torso pose lower (operator higher view).
3. Props + tasks for teleop.

### 2026-07-09

**Morning — sim test of colleague's retargeting: GOOD (with one tweak):**
- Pulled + added `scripts/setup/sync_luna.sh nvidia@6.6.7.100` to CHEATSHEET §0 (⚠️ always pass the address — script default is another robot).
- Sim test on labmate's config: right **middle finger over-sticks** → lowered its `d2` 4.0→3.0 in `wuji_right.yaml` (uncommitted). Feels good in sim now; on-robot verdict pending.
- **robo.nix change broke the open operator shell** (`No module named mujoco` — half-updated PATH after in-place re-eval). Fix: exit + re-enter `robo shell -p operator`. Env itself was fine.
- **`we data rerun` UNBLOCKED**: `rerun-sdk` was missing from the operator venv → `uv sync --extra operator --extra data-rerun --group dev --group wuji-glove`. Save-mode export verified on today's data. ⚠️ That sync dropped torch/lerobot (add `--group train-lerobot` back before any `we eval`/`we deploy`). Native-viewer lockup retest still pending (new `egl-wayland` in robo.nix may have fixed it).
- **Torso lock moved to config** (labmates): `ik_preset.joints.torso: false` in `config/teleop/pico_ee_absolute_wuji_hands.yaml` (uncommitted), NOT driver `--lock-torso`. CHEATSHEET §1 updated.

**Afternoon — teleop session (jitter recordings made, but camera-less):**
- ⚠️ Luna was synced to master only AFTER the session — recordings ran old-tree driver.
- **Jitter episodes recorded** (`teleop-bug1..3__20260709-*` + `lagging_movement.zip`) — but **ZERO camera frames** in all of them (recorder log: `image_avg=0.0KiB`, `not_ready` forever → driver served no camera). Send to team as-is or re-record with camera — decide.
- **Recording keys confirmed in code**: `r` start = ramp to init THEN record; `s`/`r` stop = save/discard + ramp to init. Hands do NOT open on reset (upstream regression still present — reset packet sends last hand pose). Task must be selected (`rc`) before `r`.
- ⚠️ New 60 Hz snapshot stream (dc100cc) → recorded state is 60 Hz; jitter above ~30 Hz will alias — tell the team when sending data.
- **Camera root causes found (2 stacked):** (1) one-off `wait_for_frames timeout` at startup = benign; (2) **cam view all WHITE = master's fixed-exposure lock** (see TODO #1) — `D405_EXPOSURE_US = 3000.0` local edit made, needs sync + on-robot tuning.
- **Labmate: the 一卡一卡/stationary-jitter is a PICO reading issue** → try repositioning the PICO on the shelf (TODO #3).

**Uncommitted local changes as of today:** `driver.py` (exposure → 100, D455 units — VERIFIED on robot, view normal), `config/teleop/pico_ee_absolute_wuji_hands.yaml` (torso false), `config/inputs/retargeting/wuji_right.yaml` (middle d2 3.0), + the 4 `act_pick_toy*.yaml`. **Exposure fix also committed on branch `wip/d455-exposure-fix-20260709` (`96cc236`)** — if a force-push reset ever nukes the working tree: `git cherry-pick 96cc236`. Real fix = upstream a `--camera-exposure` flag (D405/D455 units differ) — raise with labmates.

**Evening:**
- **Cam view NORMAL on robot** with exposure `100` ✓ (root cause was units: D455 RGB takes 100µs units, D405 takes µs — same number = 100× difference).
- **Robot moved to the right** (operator-view fix, TODO #2 done).
- PICO rack insight captured in TODO #3 (tilt → only lower two tracking cams see controllers); stationarity A/B planned for tomorrow.
- Picking up task items from Eden. Called it a day — 明天见.

### 2026-07-10

**Morning — glove extension cable attempt → both gloves down → root-caused + recovered (no extension for now):**
- Added a USB extension cable for the gloves → both unpingable (`192.168.1.100/101`). TWO stacked failures, untangled:
  1. **Right glove (.101, RTL8153 `enx00e04c681dc9`): passive USB3 extension degraded the SuperSpeed link** — kernel log full of `Tx status -71` (`-EPROTO`) → periodic USB disconnect/re-enumerate. Smoking gun: machine up since 07-07, **first -71 ever = 10:27 today, exactly when the extension went in**; zero errors after removal. Passive USB3 extensions can't carry SuperSpeed over the extra length — known failure mode.
  2. **Left glove (.100, AX88179 `enx6c1ff7c5a97d`): glove processor HUNG** (likely power glitch while re-cabling) — ethernet carrier UP at 100Mb/s but zero RX packets, ignores ARP. Cable reseats can't fix this; **power cycle did** (unplug power 5s, ~20s boot).
- **Resolution: extension removed, left glove power-cycled → both gloves 0% loss, 0 USB errors.** Decision: run WITHOUT extension for now; proper fix later (see TODO — active repeater / powered hub / ethernet-side extension).
- **Diagnostic recipe (keep):** `lsusb` = USB enumeration only; `ethtool <if>` link detected = PHY has power, NOT device alive; `journalctl -k | grep "Tx status -71"` = USB signal integrity; **carrier UP + ping dead + RX counters frozen (`ip -s link`) = device hung → power cycle**. Glove↔adapter map: R=.101=RTL8153 (`00:e0:4c`), L=.100=AX88179 (`6c:1f:f7`); workstation uses /32 + static scope-link route per adapter (intentional, don't "fix").

**Late morning — Rubik's cube teleop MUCH better with the new operator view ✓ + 一卡一卡 recorded WITH camera:**
- **Operator-view verdict (TODO #2): the 07-09 move (robot right ≡ operator right→left) clearly works** — cube task teleop a lot better this session.
- **Recorded the session incl. the 一卡一卡/stutter behavior**: `rubric-cube-0710__20260710-105655` (6 eps, 94,756 frames @ 60 Hz, 4.3 GB, **camera frames confirmed in schema** — `observation.images.egocentric` 640×480) + `lagging_movement_2.zip` (1.8 GB vs yesterday's 8 MB → camera present, unlike the 07-09 camera-less episodes). **Parquets sent to labmate for analysis** — supersedes the "send state-only vs re-record" decision. ⚠️ Remind them: state is 60 Hz sampled, jitter >30 Hz aliases.
- **`we data rerun` status (07-10 retest):** native viewer FAILS to launch in the robo env (`winit ... Failed to load one of xlib's shared libraries` — egl-wayland fix did NOT cover this). **Browser mode works**: `we data rerun <name> -e N --no-robot-model --browser`, then open `http://127.0.0.1:9090/?url=rerun%2Bhttp%3A%2F%2F127.0.0.1%3A9876%2Fproxy` (plain `localhost:9090` = disconnected welcome screen). ⚠️ **But viewer freezes after ~10 s on a full episode** (~15k frames + images) — next time try `--max-frames 2000`-ish windows or `--save ep.rrd` + open in a standalone rerun viewer outside the robo env.
