# Weekly Notes — 2026-06-23 to 2026-06-29

> Running daily log for this week. Updated each session.
> Large technical milestones have their own docs (linked when relevant).
> ← back to [overview](../../README.md)

---

## Open TODO

> Single source of truth — updated each session. Check off items when done, add new ones as they come up.

**Infrastructure / networking:**
- [x] Make glove ethernet IPs persistent via nmcli
- [ ] Fix workstation operator IP to `6.6.7.200` via nmcli (currently using `6.6.7.166` manually)
- [ ] PICO ethernet setup — have router + switch; need to connect both workstation + PICO to switch, switch to router (for DHCP). Then verify PICO ↔ workstation comms, then robot ↔ workstation.

**Hardware:**
- [x] Left glove dongle — recovered after re-plug. Monitor for recurrence.
- [ ] Wuji hand ↔ TIANJI robot mount — TIANJI's replacement flange has issues; 3D print a temporary mount ourselves (in progress 2026-06-26)
- [ ] Quest headset setup — figure out how to set it up for teleop
- [x] Quest headset — received from equipment person 2026-06-25
- [ ] Mount Wuji hand to robot — once 3D-printed mount is ready (target: Monday 2026-06-29)

**Software:**
- [x] Update `we-teleop` to master branch on workstation — 2026-06-26; resolved YAML conflicts (kept our calibration values, adopted `gate_power: 2.0`)
- [ ] Update `we-teleop` to master branch on Orin — deferred to Monday; same YAML conflict resolution needed
- [x] Impedance mode (`switch_to_imp_joint_mode`) — labmate already fixed in master (now uses `switch_to_pd_mode`); no longer blocking
- [ ] Pedal setup for data collection — check `we-teleop` repo for instructions

**Teleop quality:**
- [ ] PICO wrist calibration — redo once mounts are stable
- [ ] Hand retargeting — labmate pushed fixes (pinky misalignment + Wuji freeze) in master; test after Orin is updated

**TIANJI support:**
- [x] Circle script bug reported — error `-19` on `runtime_set_joint_pos_cmd`; awaiting reply
- [ ] Circle script SDK — TIANJI has no x86 `.so`; uploaded C++ source + compile instructions to GitHub (`cynthia-you/TJ_FX_ROBOT_CONTRL_SDK`, branch `测试用例`). Also working on ARM64 `.so` but not yet working. **Deferred: compile SDK ourselves (Monday or weekend)**
- [ ] Wuji hand flange replacement — TIANJI flange has issues; making temporary 3D-printed mount (see Hardware above)

**Next (Monday 2026-06-29):**
- [ ] 3D-printed mount ready → mount Wuji hand to robot
- [ ] Update Orin `we-teleop` to master
- [ ] Compile TIANJI SDK from source (C++ → aarch64 `.so`) and re-run circle script
- [ ] PICO ethernet setup

**In progress:**
- [~] Robot control accuracy test (TIANJI circle script) — failed with SDK version mismatch; TIANJI working on ARM64 `.so`. Awaiting their reply / SDK update.

**Next milestone:**
- [ ] First data collection run — once teleop quality is acceptable

---

## Daily Log

### 2026-06-23

**Goal for today (EOD):** teleop + data collection pipeline fully set up. *(Slipped — real robot teleop moved to tomorrow; see below.)*

**What was done:**

- `robo shell -p operator` + `uv sync --locked` + `we --help` all successful. ✓
- Qt6 6.11.0 already installed. ✓
- XRobo PC Service built and started via `bootstrap_xrobot_pc_service.sh`. ✓
  - `TELEOP_XROBOT_HOST_IP=10.5.3.177` (workstation WiFi IP — auto-detected).
- XRoboToolkit APK (`XRoboToolkit-PICO-1.1.1.apk`) installed on PICO via `adb install -g`. ✓
  - PICO connected to workstation via lab WiFi (both on `10.5.3.x`) — direct ethernet not needed.
  - XRoboToolkit app shows PICO IP `10.5.3.140`, connected to workstation service. ✓
- **Sim teleop working**: `we teleop --profile tianji_pico_ee_absolute` — MuJoCo viewer opens, robot follows PICO controllers. ✓
  - Controls: `p` = pause/resume; `Enter` (while paused) = re-anchor controller pose and resume; `r` = record toggle; `s` = save episode; `reset` = reset to initial pose.
- Wuji Studio installed (`sudo apt install ./wuji-studio*.deb`). ✓
- Orin robot-side setup (`bootstrap_luna_orin.sh nvidia@6.6.7.100`): Nix + robo installed, `uv sync` complete. ✓
  - **Blocked:** `libGentoSDKPY.so` missing — Gento SDK not compiled. Bootstrap said "already up to date" but .so was never built. Needs manual fix on Orin.
- Robot battery issue: one battery broken, affecting charging. TIANJI sending someone in person to fix.
- Wuji gloves: partial progress.
  - Right glove found at `192.168.1.101` via dongle `enx6c1ff7c5a97d` (ASIX AX88179). ✓
  - Both gloves appear to have the same IP (`192.168.1.101`) — need to change left glove to `192.168.1.100` in Wuji Studio.
  - IP setup not persistent — needs `nmcli` config (see pick-up notes).

**Ethernet adapter interface names (MAC-based, workstation):**
- `enx6c1ff7c5a97d` — ASIX AX88179 dongle (colleague's)
- `enx00e04c681dc9` — Realtek RTL8153 dongle (from hub)
- Glove IPs: left `192.168.1.100`, right `192.168.1.101`
- Workstation-side IPs: use `192.168.1.166/32` and `192.168.1.167/32` (not `/24` — causes routing conflict)

---

### 2026-06-24

**Morning:** TIANJI battery support staff arrived — spent whole morning on battery issue (one battery broken, more complex than expected). Resolved by ~1:20pm. Rest + late lunch till ~3pm. Back at desk ~3:30.

**Afternoon:**

- **Gento SDK on Orin fixed** ✓
  - `bootstrap_luna_orin.sh` had skipped compilation on second run (false "already up to date").
  - Fixed manually: `ssh nvidia@6.6.7.100` → `robo shell --profile tianji-driver` → `bash third_party/tianji/00040400/linux_auto_compile.sh`
  - `we tianji-driver --status-only` passes: connected to `6.6.7.190`, all joints (head/torso/left_arm/right_arm) IDLE. ✓
  - **Orin is fully ready for live teleop.**
- **Both Wuji gloves connected and reachable** ✓
  - Left glove: `192.168.1.100` via `enx6c1ff7c5a97d` (ASIX AX88179, ~1.3ms)
  - Right glove: `192.168.1.101` via `enx00e04c681dc9` (Realtek RTL8153, ~0.1ms)
  - Key: must use `/32` addresses + explicit host routes (not `/24`) to avoid routing conflicts.
- **Wuji glove deps installed** ✓ — `uv sync --locked --extra operator --group wuji-glove`
- **Wuji Studio calibration done** ✓ — visualization looks good.
- **Pinch calibration done** ✓ — `scripts/inputs/calibrate_wuji_pinch.py --hand left/right`; thresholds written to `config/inputs/retargeting/wuji_{left,right}.yaml`.
- **3D printed PICO controller mounts** — only half printed, using tape as interim fix.

---

### 2026-06-25

**Goal:** Get the full teleop pipeline running (arms + hand) on the real robot. ✓ (achieved)

**What was done:**

- **Full teleop pipeline working** ✓ — arms + right Wuji hand teleoperated on real robot with position control mode.
  - Orin: `export WUJI_RIGHT_HAND_SERIAL=306735523434 && we tianji-driver --operator-address 6.6.7.166 --control-mode position --auto-reset-errors --enable-wuji-hands --wuji-hand-side right`
  - Workstation: `we teleop --profile tianji_pico_ee_absolute_wuji_hands --driver-address 6.6.7.100:5559`
- Re-ran Wuji pinch calibration for right hand — thresholds updated in `config/inputs/retargeting/wuji_right.yaml`.
- Robot reset via `we tianji-driver --reset-once --acc 20 --control-mode position` before shutdown.

**Key fixes discovered during bringup:**

- Workstation operator IP was `6.6.7.166` — must match `--operator-address` flag.
- Left arm failed `switch_to_imp_joint_mode` repeatedly — workaround: `--control-mode position --auto-reset-errors`. Used 上位机 to manually reset ARM0 to 遥操姿态 during debugging.
- Wuji hand USB must be on **Orin** (not workstation) — driver controls it there.
- Wuji hand udev rule was missing on Orin (`ERROR_ACCESS -3`) — installed `70-wuji-hand.rules`, unplug/replug fixed it.
- Right hand serial is `306735523434` (not the default `348234593533`).

**Afternoon (post-meeting):**

- **PICO ethernet**: needs switch + dedicated router (for DHCP) — not a direct workstation↔PICO connection. Have both router and switch in lab now; setup pending.
- **Switched we-teleop to `refactor-06-22` branch** ✓ — git stash → checkout → stash pop to preserve calibration YAMLs.
- **Wuji glove IPs made persistent via nmcli** ✓ — both adapters configured; IPs survive replug/reboot. No more manual `ip addr add` needed.
- **Robot control accuracy test**: new task — circle-drawing script on TIANJI. Have commands from labmate (in HANDOFF); next step is `uv sync --locked` + run live.
- **Pedal for data collection**: need to set up foot pedal for efficient episode recording. Check repo for instructions.
- **Quest headset**: requested from equipment person, pending arrival.

---

### 2026-06-26

**Goal:** Run TIANJI new circle script; investigate impedance mode bug; pull latest + switch to master.

**Firmware versions collected** (queried via `GentoRobot.py` snippet on Orin, inside `robo shell --profile tianji-driver`):

```
SDK version:        00040400
Controller version: 00040403
left_arm  servo versions: ['V3.0.2.23', 'V3.0.2.23', 'V3.0.2.23', 'V3.0.2.23', 'V3.6.0.12', 'V3.6.0.12', 'V3.6.0.12']
right_arm servo versions: ['V3.0.2.23', 'V3.0.2.23', 'V3.0.2.23', 'V3.0.2.23', 'V3.6.0.12', 'V3.6.0.12', 'V3.6.0.12']
```

Script used (run from `~/we-teleop` on Orin):
```python
import sys, time
sys.path.insert(0, 'third_party/tianji/00040400/PYTHON_SDK')
from GentoRobot import GentoRobot, FXObjType
robot = GentoRobot()
robot.link(6, 6, 7, 190)
time.sleep(1)
print("SDK version:       ", robot.get_sdk_version())
print("Controller version:", robot.get_controller_version())
for label, obj in [("left_arm", FXObjType.OBJ_ARM0), ("right_arm", FXObjType.OBJ_ARM1)]:
    print(f"{label} servo versions: {robot.get_ctrl_obj_servo_version(obj)}")
```

Key finding: both arms have **identical** servo firmware — the left arm impedance failure is not a version mismatch. Likely a firmware bug specific to `ARM0`. To report to TIANJI.

**TIANJI circle accuracy script** — attempted, failed on both arms. Details:

- TIANJI provided new circle script via GitHub: `cynthia-you/TJ_FX_ROBOT_CONTRL_SDK`, branch `测试用例`, path `LUNA_TEST_PDMode_CIRCLE/TEST_SCRIPT/CARTESIAN_CIRCLE_GentoArmOnlyPD_200hz_IK.py`
- Script uses Gento PD mode (`switch_to_pd_mode`) + IK to draw a 7.5cm-radius Cartesian circle at 200Hz
- Repo only ships a Windows `.dll` — no aarch64 Linux `.so`. Workaround: symlinked we-teleop's compiled `libGentoSDKPY.so` (SDK `00040400`) and copied its `GentoRobot.py` into the repo's `PYTHON_SDK/`
- **Both arms (A and B) fail** with the same error:
  ```
  runtime_set_joint_pos_cmd failed: Format command failed (-19)
  ```
- Arm A: failed during MOVLA to first circle point (step 9); recovered to default safely
- Arm B: failed immediately at step 4 (move to default joints); did not recover (185° from default); reset manually via `--reset-once` before shutdown
- **Root cause hypothesis**: SDK version mismatch — script was written/tested with SDK `00040402` + controller `00040405`; our setup is SDK `00040400` + controller `00040403`. TIANJI notes error may be related to position→PD mode switch (similar to our impedance mode issue).
- **TIANJI's response**: no x86 `.so` available either; uploaded C++ SDK source + compile instructions to GitHub; also working on ARM64 `.so` (not yet working). Next step: compile from source ourselves.
- **Also found**: labmate already fixed the `switch_to_imp_joint_mode` failure in master — arms now use `switch_to_pd_mode` in "impedance" mode. Pulled master on workstation 2026-06-26.

---
